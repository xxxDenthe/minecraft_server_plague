# Запуск первого игрового дня: что готово и что нужно сделать

**Дата:** 2026-09-05
**Зачем:** владелец хочет начать сессию сегодня. Здесь список того, что
уже работает, и порядок действий до входа игроков.

## Что готово

- **Ядро чумы.** Эпидемия считается, хранится, видна на поверхности
  и под землёй. Баланс — `config/plaguecore-common.toml`.
- **Заражение игрока.** Здоровье, еда, кашель, тусклый экран, порча
  голоса через Simple Voice Chat, отвар, штраф за смерть.
- **Сердце чумы.** Сущность спавнится, разрушается по кускам,
  умирает. Размер — `[heart] scale`, по умолчанию 1.5.
- **Лаунчер.** Раздаёт пак, проверен на стороннем ПК.
- **Панель мастера** (`lmpc_gmtools`) — объявления, вход по паролю.

## Порядок запуска

**1. Поднять сервер.** Его до сих пор нет: заметка
`2026-09-03-pervaya-razdacha.md` заканчивается словами «появится
сервер —». Нужен NeoForge 21.1.249, JDK 21, все моды пака в `mods/`.

**2. Центр мира и барьер 1000 × 1000.** Одна команда, права оператора.
Встать в понравившемся месте и:

```
/plague center ~ ~
```

Она ставит разом: сетку чумы (63 × 63 чанка) вокруг этой точки, границу
мира на 1000 блоков и точку возрождения. Раньше это делалось тремя
ванильными командами от чанка 0,0 — теперь центр где угодно, разъехаться
им нельзя. Разбор: `2026-09-05-centr-mira-komandoj.md`.

Проверить, куда встал центр: `/plague center` без аргументов.

**3. Посеять эпидемию.** Мир начинается чистым, чума не заводится сама:

```
/plague generate 0.05        # 5 % чанков заражено, очаги случайны
/plague info                 # проверить, что счётчики поднялись
```

Полезное рядом: `/plague night` — прогнать одну ночь вручную,
`/plague pause` и `/plague resume` — заморозить эпидемию,
`/plague setphase <n>` — перескочить на нужную фазу,
`/plague gui` — карта заражения.

**4. Разложить свежие джарники в пак.** Собираются из исходников,
в релиз не качаются:

```
plaguecore/build/libs/plaguecore-0.1.0.jar
lmpc_gmtools/build/libs/lmpc_gmtools-0.17.0.jar
lmpc_shade/build/libs/lmpc_shade-0.8.0.jar
lmpc_classes/build/libs/lmpc_classes-0.5.0.jar
```

Первые три уже лежат в `mods/` свежими (2026-09-05).
`lmpc_classes` в папке всё ещё **0.4.1**, тогда как код 0.5.0 —
собрать и заменить перед раздачей.

**5. Раздать пак.** `tools/publish-pack.js`, порядок — в заметке
`2026-09-03-razdacha-paka-cherez-releases.md`.

## Дыры, которые видно прямо сейчас

**Папка `mods/` расходится со старым списком на 14 модов.** 2026-09-05
`MODLIST.md` переписан по факту папки: 87 сторонних + 4 наших. Четырнадцать
пропавших вынесены в конец списка отдельным разделом — среди них `ColdSweat`,
`CustomSkinLoader` и `tectonic`, на которые уже написаны конфиги
в `launcher/pack-config`. **Решить, вернуть их или выкинуть, до раздачи
пака:** разойтись клиенту и серверу по модам — верный способ уронить
вход всем восьмерым.

**Все четыре класса доделаны (0.7.0), но не проверены в игре.** Клирик,
Кузнец, Фермер, Летописец — способности написаны и собраны. Ни грядка,
ни очиститель, ни снимок камерой ни разу не запускались с живым игроком.
Заметка `2026-09-05-peredacha-sessii-6.md`, раздел 3.

**Финала нет.** Сердце чумы стоит и ломается, но логова, волн мобов
и очистки мира после победы не спроектировано (подсистема 6). До дня
четвёртого это не мешает.

**Живых замеров нет.** Спек обещает: игрок без брони в чанке уровня 4
доходит до стадии 3 за 6–7 минут наверху и за 5 под землёй. Никто
не мерил. Слышимость искажённого голоса на стадии 4 тоже не проверена
на людях. Первый час сессии и будет этой проверкой — держать под рукой
`config/plaguecore-common.toml` и `/plague player`.
