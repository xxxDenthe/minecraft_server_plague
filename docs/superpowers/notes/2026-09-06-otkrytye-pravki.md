# Открытые правки: что осталось сделать и кому

**Дата:** 2026-09-06, вечер
**Что это:** список незакрытого после работы над симптомами
(`plaguecore` 0.2.0) и сведением пака. Не пересказ проекта — только то,
что кто-то должен доделать, с ответственным и причиной.

**Почему файлом, а не сообщением.** Над репозиторием одновременно идут
несколько сессий Claude, у каждой своя переписка, и правят они одни
и те же файлы. Всё, о чём договорились в чате или через сообщение
соседней сессии, для следующей сессии не существует: она увидит только
репозиторий. Поэтому договорённости записываются сюда.

---

## 1. Защита кулона Клирика — закрыто, ничего делать не надо

Записано, чтобы решение не переоткрывали. Владелец 2026-09-06 поднял
`clericPendantProtection` с 0.15 до 0.25: после того как броня
в `plaguecore` стала гасить 2 % за очко вместо 1 %, полный алмаз (0.40)
начал вдвое перевешивать кулон (0.195 на третьем тире), и классовая
вещь перестала быть заметной. Третий тир теперь даёт 0.325.

Сделано целиком: дефолт в коде — коммит 99080a1 соседней сессии,
живые конфиги — `E:\CLAUDE\server\config\lmpc_classes-common.toml`
и `ModrinthApp/profiles/LMPCCHUMA/config/lmpc_classes-common.toml`.

### Заодно, чтобы не тиражировалось дальше

Разбор «при полной экипировке четверть кулона пропадает в потолке 0.9»
верен по номиналу, но не по пользе. Считать надо приращение:

```
сборка                   без кулона   сумма+потолок      «от остатка»
полный алмаз + повязка      0.750     0.900 (+0.150)   0.799 (+0.049)
полный алмаз                0.400     0.595 (+0.195)   0.517 (+0.117)
только повязка              0.350     0.545 (+0.195)   0.477 (+0.127)
```

Упор в потолок означает, что игрок уже на максимуме защиты, и кулон
в этот момент даёт больше, чем дал бы в любой схеме «множитель на
остаток». Нынешняя схема (слагаемое плюс потолок 0.9) — самая щедрая
к кулону из возможных; переделывать её не нужно.

## 2. Ждёт владельца

- ~~**Текстура повязки**~~ — **закрыто**: своя текстура
  `plaguecore:item/plague_mask` и слой поверх модели игрока сделаны
  соседней сессией в коммите `9db569a`; ванильной попоны в
  `assets/plaguecore/models/item/plague_mask.json` больше нет.
  Отвар тоже получил свою `plaguecore:item/plague_brew`.
- **Где жить скрипту замера.** `zamer_zarazheniya.py` лежит в скретчпаде
  сессии и в репозиторий не попал: место не выбрано. Варианты —
  `launcher/tools/`, `tools/`, либо рядом с сервером вне репозитория.
  Описание — `2026-09-06-protokol-zhivyh-zamerov.md`.
- **Спек ядра против сборки.** Спек обещает до стадии 3 шесть-семь минут
  наверху и пять под землёй (`2026-09-03-plague-core-design.md`,
  строка 465). По константам выходило 5.0 и 3.7, а после симптомов —
  4.0 наверху с учётом вдохов спор. Варианты (поправить спек, подтянуть
  числа, развести осознанно) лежат в конце
  `2026-09-06-protokol-zhivyh-zamerov.md`; решение не принято.
- ~~**Белый список сервера.**~~ **Закрыто 2026-09-11 решением
  владельца: белого списка не будет, пускает Radmin.** На
  `E:\CLAUDE\server` осталось `white-list=false`,
  `enforce-whitelist=false`, а `enable-rcon` переведён в `false`
  и обратно не нужен.

  Разбор, чтобы это не переоткрывали. При `online-mode=false` сервер
  вычисляет игрока прямо из ника, поэтому белый список проверяет ник,
  а не человека: подделавший ник проходит его насквозь. Фильтрует
  по-настоящему пароль Radmin-сети — чужой трафик до сервера просто
  не доходит. Идею «лаунчер сам дописывает игрока в белый список
  по введённому нику» владелец поднимал 2026-09-11 и после разбора
  отклонил: список лежит на машине сервера, лаунчер — на машине
  игрока, и чтобы запись работала, сервер должен принимать «впиши
  меня» от любого в сети — то есть список перестаёт ограничивать
  что-либо и превращается в выключенный, но с лишней службой.
  Вариант «очередь на подтверждение владельцем» оставлен нетронутым
  на случай, если список когда-нибудь понадобится.

  **Операторов на сервере нет:** `ops.json` пуст, GM-инструменты
  `lmpc_gmtools` требуют уровня 2, так что `/gamemode`, `/tp` и
  GM-панель зашедшему недоступны. Но опасность прямо следует из
  `online-mode=false`: как только оператор появится, его ник сможет
  назвать любой. Поэтому оп берётся из окна сервера на время и
  снимается после.

## 3. Ждёт живой игры

Ни один симптом из `plaguecore` 0.2.0 не видел живого игрока: приступы
кашля, усталость, жажда, вдохи спор, повязка. Порядок проверки и скрипт —
`2026-09-06-protokol-zhivyh-zamerov.md`, разбор самих симптомов —
`2026-09-06-simptomy-dnyom.md`.

Прицельно посмотреть:

- **Слот Curios.** `plaguecore` объявил игроку слот `head`. Если
  окажется, что за него уже дерётся что-то из пака, повязку проще
  перенести в `charm`, чем спорить за место.
- **Тошнота на стадии 2** — самый спорный эффект, читается скорее как
  отравление, чем как чума. Снимается одной строкой
  (`PlagueConstants.COUGH_NAUSEA_STAGE`).
- **Вдохи спор** ускоряют заражение примерно на четверть. Если это
  ощущается как скрытый сдвиг порогов, `gustChance` ставится в ноль,
  и остаются только симптомы.
- **Сила повязки** 0.35 даёт окупаемость подготовки в 3.6 раза.
  Ровно вчетверо — `maskProtection` 0.40.

## 4. Мелочи, за которыми никто не закреплён

- **Повязка без Curios** крафтится, но не надевается. Жёсткой
  зависимости у `plaguecore` нет и не будет; если Curios когда-нибудь
  выпадет из пака, повязку придётся переносить в слот брони.
- **Кодовый блок `mods/MODLIST.md` собран из папки** скриптом
  (коммит a08f663). Следующему, кто будет его править, проще
  пересобрать так же, чем дописывать строки руками.
- ~~**Долг из передачи седьмой**, не тронутый сегодня: электрический тир
  очистителя и потребление стресса Create (за сессией классов),
  пограничье подсистемы 4, лор дальше тайнописи, финал дальше Сердца,
  план автообновления лаунчера.~~ **Список устарел, 2026-09-10.**
  Владелец разобрал его по пунктам:

  - **электрический тир очистителя отсекается полностью** — тира не
    будет, задачи нет;
  - **лор на тайнописи больше не завязан** — «лор дальше тайнописи»
    как долг не существует, актуальное состояние в
    `2026-09-07-lor-revizia-2.md`;
  - **пограничье подсистемы 4 закрыто** кодом того же дня —
    `2026-09-10-pogranichye-kod.md`: приметы, лут по глубине, ночной
    прилив. Открытым остался только форпост, и то как «на рассмотрение»;
  - **автообновление лаунчера и новости** делает соседняя сессия,
    план — `plans/2026-09-09-avtoobnovlenie-i-novosti.md`;
  - про финал дальше Сердца владелец ничего не сказал, пункт висит.

## 5. Модели и Blockbench (добавлено 2026-09-07)

- ~~Гнездо вала у андезитового очистителя вдвое шире канона.~~
  **Закрыто 2026-09-07 вариантом Г1** — владелец сказал «убрать
  текстурки вала». Свои гнёзда обоих тиров удалены, модели ссылаются
  на `create:block/gearbox` и `create:block/brass_gearbox`, а ниша
  под гнездом собрана по эталону Create.
- ~~Форма андезитового очистителя переделывается.~~ **Закрыто
  2026-09-07** — в мод выкачен вариант A («корпус машины»), исходник
  `textures_src/create_sprayer/andesite_purifier.bbmodel`.
- ~~Латунный очиститель: силуэт остался старым.~~ **Закрыто
  2026-09-07** — владелец распорядился перенести форму A на латунь
  и сделать ей свой пояс. Пояс и плита получены переносом правки
  Create с андезитовой оправы на латунную, скриптом.
- ~~**Кодекс класса — единственная иконка на ванильной текстуре.**~~
  **Закрыто 2026-09-07:** своя `lmpc_classes:item/class_codex` —
  тёмный зеленовато-бирюзовый том с закладкой, **нарисован владельцем**
  и отдан файлом `grimuar.png`. Сгенерированный скриптом коричневый том
  с ремнём, стоявший там несколько минут, заменён вместе со своим
  генератором: держать мёртвый код рядом с живой текстурой незачем.
  Оригинал и разбор — `textures_src/class_codex/`. Остальные заглушки иконок закрыты в тот
  же день соседней сессией: кадило получило свою
  `lmpc_classes:item/censer`, реагент и отвар Клирика переехали
  на готовые `create:item/antioxidant` и `create:item/builders_tea`,
  недособранный очиститель — на `create:block/andesite_casing`.
  Ванильных заглушек в иконках мода больше нет.
- ~~**Электрический тир очистителя** не существует ни как блок, ни как
  облик. Когда дойдёт — оправа берётся готовой у `createaddition`,
  а не рисуется (`2026-09-06-latunnyj-ochistitel-oblik.md`).~~
  **Отменено владельцем 2026-09-10: электрического тира не будет
  вовсе.** Ни блока, ни облика рисовать не надо.
- **Живьём обликов не видел никто** — ни латунный на настоящем свете,
  ни сопло, торчащее на пиксель под блоком, поставленным сверху.
  Заодно стоит пересмотреть кадр Ponder-сцены латунного: модель
  в сцене теперь другая.
- Как работать с Blockbench и что считать каноном Create —
  `2026-09-07-blockbench-create-style-rukovodstvo.md`.

Подробности по закрытым пунктам —
`2026-09-07-port-vala-i-forma-ochistitelya.md`.


---

## 6. Долги выбывшего второго разработчика (добавлено 2026-09-11)

Второй разработчик выбывает из разработки. За ним оставались две
половины подсистемы 7; первая сделана, вторая не начата.

**Автообновление лаунчера и новости — сделано**, разбор в
`2026-09-10-avtoobnovlenie-i-novosti-sdelano.md`. Не проверено живьём:
полный круг обновления (поставить старую версию, выложить новую,
убедиться в тихой установке) и первая настоящая выкладка новостей —
настоящего токена в сессии не было.

**Автоподключение к серверу — не начато**, за исключением каркаса,
который уже в коде (`launch.js:133`, ванильный `--quickPlayMultiplayer`;
`manifest.js` разбирает поле `server`; `publish-pack.js --server` умеет
его записывать). Чтобы игра начала заходить сама, сегодня достаточно
выложить пак с `--server хост:25565`. Подробности и развилки —
`2026-09-09-avtopodklyuchenie-zapiska.md`, там же не начатые строка
состояния сервера на главном экране и запись адреса в `servers.dat`.

Владелец на 2026-09-11 решил перед запуском закрыть только раскладку
Пограничья и безопасность сервера; автоподключение сознательно
отложено.

## 7. Пограничье разложено (добавлено 2026-09-11)

Код Пограничья лежал в `main` с 2026-09-10, но собранный джарник
никуда не попал: в `mods/`, профиле `LMPCCHUMA` и `server/mods` стоял
`plaguecore-0.2.0.jar` от 2026-09-07, то есть **новых механик в игре
не было**, и по имени файла это не читалось — номер версии совпадал.

2026-09-11 версия поднята до **0.3.0**, сборка зелёная, джарник
разложен во все три места, старый 0.2.0 оттуда убран, `mods/MODLIST.md`
приведён к факту. Пак после этого надо перевыложить, иначе игроки
получат старое ядро.


## 8. Пак выложен, версия 2 (добавлено 2026-09-11)

Раздача с ядром `plaguecore-0.3.0` ушла игрокам: `packVersion` 2,
перезалиты `mods.zip`, `config.zip` и `CustomSkinLoader.zip`, остальные
два архива не изменились. Порядок был обычный —
`sync-profile.py`, `build-pack-from-profile.py`, `publish-pack.js`.

Две мелочи, стоившие времени и пригодные следующему:

- **`publish-pack.js` не берёт `--token` из командной строки**, пока
  нет `launcher/publish.json`: без файла он падает с «нужен токен»,
  даже когда ключ передан. Файл лежит в `.gitignore`, в нём же и токен.
- **`dir` в `publish.json` резолвится от корня репозитория**, поэтому
  правильное значение `launcher/pack-build`, а не `pack-build` из
  образца в README.

**Адрес сервера в манифест не вписан.** Владелец 2026-09-11 дал адрес
Radmin `26.119.55.84`, но сам усомнился в порте: у «открыть мир для
сети» Minecraft берёт случайный порт каждый запуск, и тогда адрес
в манифесте протух бы через один сеанс. Фиксированный 25565 верен
только для выделенного сервера (`start.bat`). Пока не подтверждено,
каким из двух способов сервер поднимается, автозаход отложен: без
поля `server` пак работает, игрок добавляет сервер руками. Дописать —
одна перевыкладка, архивы второй раз не поедут.
