# Интерфейс «Состояние здоровья» — сделано, 2026-09-14

Пятнадцать задач плана закрыты, все ревью чистые. Эта заметка приходит
на смену `2026-09-14-sostoyanie-zdorovya-peredacha.md` — ту не трогаем,
живое из неё перенесено сюда.

## Что сделано

Экран по клавише Y показывает игроку фигурку с подсвеченными частями
тела, здоровье и голод сердцами, вкладки «Части тела / Память /
Ощущения», описания без чисел и терминов болезни. Строка на HUD в левом
верхнем углу коротко называет самочувствие. Вкладка «Память» ведёт
журнал последних событий (ухудшение, улучшение, зелье, обморок).
Наведясь на другого игрока и нажав Y, видно его же экран — сервер
проверяет дистанцию (8 блоков) и тот же мир, иначе молчит. Если жив
Клирик (`lmpc_classes`), под описанием части тела появляется вторая
строка с его репликой. Пока телом правят чужие руки (одержимость,
`PossessionClient.ведут()`) экран подёргивается и по краям проступают
короткие фразы-вторжения; фон под панелью размыт, сама панель — нет.
От одной лишь высокой стадии, без одержимости, экран не трясётся —
по краям панели проступает только гниль, начиная со ступени 2.

## Где что лежит

```
core/Wellbeing.java              чистая логика: состояние → ключ локализации
client/HealthSense.java          сбор и кэш состояния + журнал памяти
client/HealthScreen.java         экран: модель, части тела, вкладки, искажение
client/HealthHud.java            строка в левом верхнем углу
client/HealthKeys.java           клавиша Y и что по ней происходит
client/HealthMemory.java         кольцо на 12 записей журнала событий
client/PossessionClient.java     флаг «телом правят» и его источник для искажения
client/PlagueClientAccess.java   единственная точка входа клиента, приём пакетов
mc/PlagueNetwork.java    +2 записи: Look и Impression, версия протокола 5 → 6
mc/ClassBridge.java      публичный, +класс()
mc/ThirstBridge.java     публичный, +уровень()
assets/plaguecore/lang/{ru_ru,en_us}.json         все строки интерфейса
assets/plaguecore/textures/gui/health_icons.png   капля и значки вкладок
tools/health-gfx/icons.py                         генератор этого атласа
```

## Ключевое решение

Для осмотра себя новой сети не понадобилось: стадия, здоровье, голод,
эффекты, одержимость и класс уже есть на клиенте. Новых пакетов два
(`Look`, `Impression`), оба только под осмотр соседа; версия протокола
поднята с `5` до `6`.

## Долг второму участнику

В `launcher/pack-config/config/lmpc-default-options.txt` нужна строка
`key_key.plaguecore.health:key.keyboard.y` и запись в `KEY_MIGRATIONS`
лаунчера — иначе клавиша не доедет до тех, кто уже играл (разбор —
заметка `2026-09-12-posle-pervogo-testa-klavishi-i-drop`). Папка
`launcher/` чужая, правка не делалась, только передаётся этой заметкой.

## Находка вне нашей работы

`mc/ArchiveRecordItem.java:70` ссылается на клиентский класс
`client.ArchiveRecordScreen` напрямую, в обход `PlagueClientAccess`.
Код прежних коммитов, к интерфейсу здоровья отношения не имеет, но
правило «на выделенном сервере клиентские классы не грузятся» он
нарушает. Решение за владельцем.

## Что не проверено живьём

Закрыто живой проверкой на сегодня только одно: двойное размытие фона
починено коммитом `07d6b8e`, владелец открыл экран и подтвердил — «всё
работает по задумке».

Всё остальное честно висит непроверенным:

- выделенный сервер: `NoClassDefFoundError` и клиентские классы
  в логе, вход клиентом, осмотр себя (шаг 2 брифа);
- запуск без `lmpc_classes` и мода жажды: экран открывается, строки
  жажды и Клирика нет, в логе нет исключений от мостов (шаг 3 брифа);
- геометрия частей тела на фигурке — совпадают ли области попадания
  с руками и ногами, числа брались прикидочные;
- вид значков вкладок, особенно третий («Ощущения») — на десятикратном
  увеличении волны могут читаться хуже остальных и слиться на
  12 пикселях;
- не налезает ли строка HUD на значки голосового чата в том же левом
  верхнем углу;
- осмотр соседа на двух живых клиентах — нужны `/plague setstage 3`
  и проверка границы в восемь блоков;
- вход старым клиентом с протоколом 5 — что именно его отбивает
  и остаётся ли сервер жив;
- запись «Памяти» о зелье — срабатывает ли ровно на допитии; само
  совпадение по `имя.contains("plague_brew")` с настоящим id предмета
  никто не сверял;
- всё из задачи 13 (Клирик) — вторая строка описания, 45 текстов
  на две реплики, ни разу не открывались в игре;
- всё из задачи 14 (гниль и тряска) — тряска панели, рывок модели,
  вторжения по краям, ни разу не открывались в игре.

## Открытые вопросы

Из раздела 16 спека:

- точные доли высоты для попадания по частям тела — подбирались
  глазом при первом запуске, в спеке прикидочные; живой проверкой
  не закрыт;
- пересечение HUD со значками голосового чата в левом верхнем углу —
  не закрыт;
- звуки открытия экрана и выбора части тела — в этот план не входили,
  не сделаны, не закрыт.

## Решения по ходу работы (для памяти)

- `протянуть` — четырёхпараметрический:
  `протянуть(GuiGraphics, Component, int y, int цвет)` → int.
- Правило «в `render` ничего не создаётся» (спека, раздел 14) код плана
  нарушал пять раз — в задачах 8, 9, 10, 12, 13, 14; каждый раз чинилось
  одинаково: готовое держится в поле, на кадре не собирается.
- Каждый новый ключ текста заводится в трёх местах: `ru_ru.json`,
  `en_us.json` и `LangCoverageTest.всеКлючи()`.
- Капля жажды в атласе лежит по X = 48, ширина 7.

## Отложенные мелочи

Ни одна не блокирует работу. Две мелочи задачи 12 (замороженная модель
ушедшего соседа, `принятьВпечатление` поверх любого экрана) починены
финальным кругом правок после ревью ветки — из списка убраны.

- задача 2: параметр `Wellbeing.ощущение` назван `идентификатор`,
  в брифе — `идентификаторЭффекта`;
- задача 5: кириллица в `print()` генератора значков выводится
  крякозябрами в консоли Windows;
- задача 6: фолбэк `Component.empty()` в `часть()` создаёт объект
  в узком окне до первого тика;
- задача 9: `протянуть()` и `строкаСоЗначком()` дублируют цикл переноса
  строк;
- задача 9: `HealthSense.жажда()` зовётся дважды подряд;
- задача 9: `HealthScreen` вырос с ~258 до 802 строк (правки финального
  круга добавили подписи вкладок); после живой проверки стоит подумать
  о выносе правой панели в отдельный класс;
- задача 14: хит-тест частей тела не учитывает сдвиг дрожи и рывка;
- задача 14: один `Random` обслуживает дрожь, рывок и вторжения одним
  потоком вызовов — значения коррелируют;
- финальное ревью: `font.split` в `протянуть()`/`строкаСоЗначком()`
  собирает список на каждом кадре;
- финальное ревью: около 20 членов `HealthScreen` — `protected` без
  единого наследника, и хук `выбрана(Часть)` туда же — сузить до
  `private`;
- финальное ревью: `ClassBridge.класс()` публичен, наружу нужен
  только `клирик()`;
- финальное ревью: счётчик вторжений продублирован — `ВТОРЖЕНИЙ = 6`
  в `HealthScreen` и `в < 6` в `LangCoverageTest`;
- финальное ревью: мёртвая проверка `цель.level() != кто.level()`
  в `осмотреть()` (`PlagueNetwork`) — сервер и так не пускает пакет
  дальше по дистанции;
- финальное ревью: взгляд на игрока дальше восьми блоков по вертикали
  не делает ничего — ни ответа сервера, ни своего экрана.

## Живая проверка 2026-09-16

Экран впервые запущен в дев-клиенте (`gradlew runClient`) и открылся
без ошибок. Нашлась одна настоящая поломка: рамка выделения части тела
появлялась только на руках. Причина не в геометрии — зоны всех четырёх
частей считались верно (`HEAD x 390..410 y 235..255`, `TORSO x 390..410
y 255..286`, `ARMS x 379..390` и `410..421`, `LEGS x 390..400` и
`400..410 y 286..317` при экране 960×540) — а в глубине: сущность
рисуется на `z = 50`, `fill` уголков шёл на `z = 0`, и модель съедала
рамку везде, где силуэт её перекрывал. У рук уголки торчали мимо
силуэта, поэтому казалось, что работают только они. Подъём по Z убран
внутрь `уголки()`.

Заодно по решению владельца рамка стала белой и рисуется только под
курсором — у выбранной части уголков больше нет.

## Сборка

`.\gradlew.bat build` из `plaguecore/` — BUILD SUCCESSFUL, 230 тестов
в 26 сьютах, ни один не сломан.
