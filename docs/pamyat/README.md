# Память сессий Claude

Копия личной памяти Claude владельца (`~/.claude/projects/.../memory/`),
выложена сюда 2026-09-08, чтобы напарник видел и мог продолжить.

Это **копия, а не источник**: живые файлы лежат вне репозитория, и Claude
читает их оттуда. Правки здесь на его поведение не влияют — если что-то
устарело, скажи владельцу, он поправит оригинал.

`MEMORY.md` — оглавление, по строке на факт.
