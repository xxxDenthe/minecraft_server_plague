# lmpc_classes — система классов

Пятый мод репозитория (после `plaguecore`, лаунчера, `lmpc_gmtools`,
`lmpc_shade`). Отдельный Gradle-проект, свой `modId = lmpc_classes`.
Владелец этой половины — Kuragane (как `launcher/`, `lmpc_gmtools`,
`lmpc_shade`). `plaguecore/` — друга, не трогать.

## Что это

Собственная система классов вместо Origins — подсистема 3 дорожной
карты. Дизайн (роли Клирика, Кузнеца, Фермера, Летописца, механика
путёвок, интеграция с чумой) — `docs/superpowers/specs/
2026-09-04-klassy-design.md` в корне репозитория.

## Границы и платформа

- Отдельный Gradle-проект, свой `modId`, конфликтов при
  `git pull --rebase` с `plaguecore` не создаёт.
- **Minecraft 1.21.1 / NeoForge 21.1.249 / JDK 21**, ModDevGradle
  2.0.146, Gradle 8.14.5 через wrapper, пакет `dev.denthe.classes`.
- **Мод и клиентский, и серверный** (`side = "BOTH"`), как gmtools:
  предметы и вложение нужны на обеих сторонах, команды и логика смены
  класса — на сервере.
- **Мост к `plaguecore` — только мягкий, через рефлексию**, тем же
  приёмом, что `ShadeApi`/`ShadeAccess` между `lmpc_shade` и
  `lmpc_gmtools`: жёсткой Gradle-зависимости между джарами нет, нет
  мода-донора — заглушка/не сработало, но не краш. Работает в обе
  стороны: `PlagueBridge.java` здесь читает и правит заражённость
  игрока, `ClassesApi.java` здесь — то, что `plaguecore` дёргает
  обратно (`ClassBridge.java` там) ради защиты от кулона.
  Правка `PlayerInfection.защита()` в `plaguecore` сделана с явного
  разрешения владельца, заметка — `docs/superpowers/notes/
  2026-09-04-most-classes-plaguecore.md`.
  **С момента, когда друг завёл `dev.denthe.plaguecore.mc.PlagueApi`
  (официальную точку входа для классов, спек ядра 9.6),
  `PlagueBridge` рефлексирует именно на него** (`cure`,
  `grantImmunity`), а не на `PlayerPlagueData`/`PlayerInfection`
  напрямую, как было в 0.3.0–0.4.0. Три причины: контракт `PlagueApi`
  устойчивее (он для нас и задуман), не нужно тащить текущую
  заражённость отдельным полем — `cure` сам её читает, а
  `grantImmunity` не укорачивает уже идущий иммунитет (было — просто
  перезаписывало поле).
- **Curios — обязательная зависимость, честно.** Задумывалась
  «мягкой», как мост к `plaguecore`, но не вышло: `ClericsPendantItem
  implements ICurioItem` напрямую, и без Curios на classpath JVM не
  может загрузить сам класс предмета — падает вся регистрация мода,
  проверено (`Failed to create mod instance ... ClassNotFoundException:
  ICurioItem`). `neoforge.mods.toml` помечает `curios` как `required`,
  не `optional`. Для своего dev-сервера класть
  `curios-neoforge-*.jar` в `run/server/mods/` вручную — `run/`
  в `.gitignore`, никуда не коммитится.

## Устройство (0.13.0)

**Привод в сценах Ponder стал настоящим, утренний отчёт — надёжным.**
Разбор — `docs/superpowers/notes/2026-09-06-vodyanoe-koleso-v-ponder-i-zarya.md`.

- **В сценах вместо творческого мотора водяное колесо** в водном жёлобе:
  у андезитового — колесо и вал, у латунного дальше идёт контроллер
  скорости с большой шестернёй, на которой стоит сам очиститель.
  Игрок видит механизм, который ему предстоит собрать. Как собирается
  колесо, показывает сцена внутри Create, дублировать её незачем.
- **Ponder-мир не считает поток воды**, поэтому колесо крутится не само:
  скорость кладётся в NBT блок-сущности ключом `Speed` (метод
  `крутить(...)`, класс сущности намеренно `BlockEntity`, чтобы не
  тянуть жёсткую зависимость на Create).
- **`scaleSceneView(float)` задаёт масштаб, а не прибавляет к нему.**
  Отрицательное число вывернуло бы сцену; латунной башне поставлено
  `0.8f`.
- **Утренний отчёт Кузнеца копится и уходит один раз за ночь**, через
  60 тиков после последнего отработавшего очистителя, и приходит за
  каждую ночь, а не только за удачную. Раньше он молчал при неудачном
  броске и докладывал от лица первого проснувшегося блока — отсюда
  «сообщение приходит через раз». Вид: золотой заголовок, зелёная или
  серая строка итога, красная строка про кончившийся реагент, звон
  только Кузнецу.

## Устройство (0.12.0)

**Курильница, форсаж Кузнеца и очиститель в линии Create.** Разбор —
`docs/superpowers/notes/2026-09-06-kurilnica-i-forsazh.md`.

- **Курильница** (`CenserItem`) — ручная очистка под землёй, спек ядра
  10.3. ПКМ полторы секунды, тратит один `cleansing_agent` и единицу
  прочности, чистит свой чанк. **Под открытым небом не работает**, иначе
  подменяла бы машину. Главная её польза — сопротивление чанка, а оно
  тает само. Класса не спрашивает: «уровень, доступный без класса»
  (спек классов 2.1). Крафт — цепь над фонарём, текстура одолжена
  у фонаря.
- **Форсаж Кузнеца** — ответ на открытый вопрос спека 13 («активка
  Кузнеца точно ноль?»). Гаечный ключ Create по своему очистителю:
  ближайшая ночь идёт по площади тира выше и вчетверо дороже реагентом.
  Список ключей — в конфиге (`smithOverdriveItems`), жёсткой зависимости
  на Create нет. Флаг пишется в NBT: оплаченный форсаж не теряется при
  перезапуске. У Кузнеца пятая строка способностей,
  `ClassLore.способностей(SMITH)` = 5.
- **Очиститель принимает реагент лентой и воронкой** (`ClassCapabilities`,
  `ворота()`), только на вход — вытащить заряд нельзя. Рядом стоящий
  **сравнитель** показывает остаток.
- **Скорость вала больше не только порог:** до +50 %
  (`purifierSpeedBonus`) на 128 об/мин — частичная замена
  непотреблённому стрессу.
- **Столбик частиц над крышкой докладывает об уровне своего чанка**
  (дым / облако / белые искры), утром Кузнец получает строку
  об отработанной ночи.

## Устройство (0.11.0)

**Конвейер вместо чана и сцены размышления (Ponder) у обоих
очистителей.** Разбор — `docs/superpowers/notes/2026-09-06-ponder-i-konvejer.md`.

- **Андезитовый собирается на сборочной линии** (`create:sequenced_assembly`):
  корпус → депонер с медным листом → депонер с реагентом → пресс,
  два круга. Появился переходный предмет
  `incomplete_andesite_purifier` — этого требует сам Create, крафта
  у него нет. Депонер — латунный уровень, то есть первый тир стал
  дороже буквы спека 10.1; решение владельца, откат (заменить
  `deploying` на `pressing`/`filling`) описан в заметке.
- **Ponder-сцены у обоих очистителей**, по клавише W на предмете:
  7 шагов у андезитового, 6 у латунного — что делает, откуда
  вращение, сколько об/мин, куда реагент, что светится в работе,
  какая площадь, кто ставит.
- **Ponder — джар-в-джаре внутри Create**; `build.gradle` достаёт его
  в `build/ponder/` и компилируется `compileOnly`. Зависимость
  осталась мягкой: `PonderHook` не упоминает Ponder в подписях
  и зовёт плагин только под `ModList.isLoaded("ponder")`.
- **Постройка сцены — настоящая структура** `assets/lmpc_classes/ponder/*.nbt`,
  собирается скриптом `tools/ponder_structures.py` (свой писатель NBT).
- **Текст сцен — в языковых файлах** (`lmpc_classes.ponder.<сцена>.text_N`).
  Порядок вызовов `.text(...)` в Java менять нельзя, не поправив
  номера в `ru_ru.json`/`en_us.json`: номер приходит от порядка.
- Живьём не проверено ничего: в дев-окружении нет ни Create,
  ни Ponder, сцены живут только на клиенте.

## Устройство (0.10.0)

**Два тира очистителя, крафты разведены по тяжести Create.** Разбор —
`docs/superpowers/notes/2026-09-06-latunnyj-ochistitel.md`, механика
Create простыми словами — `2026-09-06-create-mehanika-i-processy.md`.

- **Андезитовый полегчал:** `create:compacting` (чан + механический
  пресс) вместо девяти механических сборщиков. Корпус + 2 медных
  листа + фильтр + реагент. Требование спека 10.1 «первый тир
  собирается водяным колесом и валом» выполнено буквально.
- **Латунный забрал прежний рецепт:** `create:mechanical_crafting`
  3 × 3 на латуни, с точным механизмом.
- **Латунный тир сделан целиком:** радиус 3 (7 × 7 чанков), скорость
  от 32 об/мин, 4 реагента за ночь, свечение 9, **ставит только
  Кузнец** — `getStateForPlacement` возвращает `null` не-Кузнецу,
  блок не тратится. Три новые ручки конфига, все в `/lmpcclasses tune`.
- **Ни второго класса блока, ни второй сущности:** тот же
  `PurifierBlock` и тот же `PurifierBlockEntity`, зарегистрированные
  второй раз; тир читается из состояния (`PurifierBlock.латунный`),
  различия — в числах конфига. Ключ типа сущности оставлен старым
  (`andesite_purifier`), иначе обнулилось бы содержимое уже
  поставленных блоков в мире владельца.
- **Долг:** текстуры латунного одолжены у Create
  (`create:block/brass_casing`), своих нет.

## Устройство (0.9.0)

**Крафт очистителя уехал в Create, эффекты стали видны.** Разбор —
`docs/superpowers/notes/2026-09-06-ochistitel-kraft-v-create-i-effekty.md`.

- Рецепт — `create:mechanical_crafting` 3 × 3 (девять механических
  сборщиков): андезитовый сплав, фильтр, трубы, латунный корпус,
  насос и **точный механизм** как главный гейт цены. Верстак больше
  не годится. Файл обёрнут в `neoforge:mod_loaded: create`, иначе
  dev-сервер без Create писал бы ошибку разбора.
- Ставить и заряжать очиститель по-прежнему может кто угодно, но
  собрать его теперь должен кто-то знающий Create — правка сделана
  по прямой просьбе владельца, требование спека 10.1 ей известно.
- Реагент (`cleansing_agent`) не тронут, крафтится руками.
- Блок получил ванильное `lit` и свечение 7 в работе; число и разлёт
  брызг и тон гула теперь зависят от скорости вала (потолок 256).
  Добавлены пар, искра по ободу, бульканье в противофазе к гулу,
  кольцо частиц по краю обработанной области на ночной работе
  (удачная ночь звучит отдельно) и хлопок на последнем реагенте.

## Устройство (0.8.0)

**Очиститель работает по площади и обгоняет ночной рост.** Живая
проверка показала, что уровень чанка застывает: ночное распространение
поднимает каждый заражённый чанк на +1, очиститель снимал ровно один —
итог за сутки ноль. Плюс одиночный чанк всё равно выглядел гнилым (вид
берётся по максимуму соседей) и упирался в правило 10.2 «нельзя
окопаться», потому что соседей никто не чистил. Разбор и арифметика —
`docs/superpowers/notes/2026-09-06-ochistitel-oblast-i-skorost.md`.

- `purifierRadiusChunks` (умолчание **1**) — квадрат 3 × 3 чанка.
- `purifierLevelsPerNight` (умолчание **4**) — попыток снять уровень
  с каждого чанка за ночь, каждая со своим броском.
- `purifierCleansePower` 0.30 → **0.85**. Спековые 0.30 при одной
  попытке ровно отыгрывали ночной рост назад.
- Реагент по-прежнему один за ночь, не по чанку (`ponytail:`);
  сопротивление подсыпается один раз, на первой попытке.
- `plaguecore` не тронут — всё уместилось в блок очистителя.

## Устройство (0.7.2)

**Тир мастерства наконец что-то значит у всех четверых.** Разбор,
числа и решения — `docs/superpowers/notes/2026-09-06-chto-daet-tir-masterstva.md`.

- **Клирик** — пассивная регенерация полсердца раз в 30 ⁄ 15 ⁄ 10 с
  (`clericRegenIntervalTicks`, делится на тир). Работает на голодный
  желудок: ванильная требует сытости, а чума сытость режет.
- **Кузнец** — шанс сковать уже зачарованную вещь (8 ⁄ 16 ⁄ 24 %,
  ванильный подбор со стола по тегу `in_enchanting_table`, уже
  зачарованное не трогается) и опыт за крафт и плавку, начиная
  со второго тира (`(тир − 1)`).
- **Фермер** — щедрый урожай: 10 ⁄ 20 ⁄ 30 % шанс на +1 предмет
  в каждой стопке дропа. Ловится `BlockDropsEvent`, поэтому
  складывается с Удачей и чужими модами вместо спора с ними.
- **Летописец** — прибавка к скорости бега 5 ⁄ 8 ⁄ 15 %, transient-
  модификатор атрибута: навешивается тиком, снимается при смене
  класса, в сейв не пишется.
- Два числа поправлены: `purifierCleansePower` 0.35 → **0.30**
  (иначе Кузнец тира I не отличался от партии без Кузнеца) и
  `chroniclerSnapshotRadiusChunks` 4 → **6** (иначе округление
  давало 4 ⁄ 5 ⁄ 5, и тир III не отличался от тира II).
- Новые способности умножаются на **тир**, а не на
  `masteryPowerPerTier`: тот даёт +15 % и на глаз незаметен.
  Старые оставлены как были — трогать их баланс без живой проверки
  опаснее.
- У каждого класса в гримуаре появилась четвёртая строка
  способностей (`*.ability.4`), `ClassLore.способностей` = 4.

**Дев-серверу нужны две вещи, которых нет в репозитории:** джарник
Curios в `run/server/mods/` (мод требует его жёстко) и
`run/server/server.properties` с портом, отличным от 25565.

## Устройство (0.7.1)

**Мастерство Кузнеца и Летописца растёт от другого — по правке
владельца 2026-09-06.** Разбор и числа —
`docs/superpowers/notes/2026-09-06-masterstvo-kuzneca-i-letopisca.md`.

- **Кузнец.** Наковальня убрана (`smithMasteryPerRepair` удалён).
  Теперь: ночная очистка чанка (как было), каждый скованный предмет
  с прочностью (`smithMasteryPerCraft`), выплавленное в печи
  (`smithSmeltsPerMastery` предметов на очко, округление вниз —
  вынос по одному не даёт ничего).
- **Летописец.** Добавлено разгаданное слово тайнописи
  (`chroniclerMasteryPerCipher`). Награду выдаёт `plaguecore`:
  только он знает, что слово раскрылось впервые. Мост —
  `ClassBridge.наградитьЗаШифр` → `ClassesApi.rewardCipher(Player, int)`,
  метод ищется отдельным `try`, чтобы старая сборка соседа не роняла
  весь мост.
- Пассивный самопочин снаряжения не тронут: это пассивка, а не
  источник мастерства.

## Устройство (0.7.0)

**Три заглушки 0.6.0 заменены настоящими способностями из спека.**
Владелец 2026-09-05 снял запрет на правку `plaguecore` — это и было
тем, что держало Кузнеца, Фермера и Летописца в заглушках: все три
упираются не в игрока, а в местность, а местность жила за закрытым
интерфейсом.

### `PlagueApi` научился отдавать чанки (правка в `plaguecore`)

Только добавлением, поведение чумы не изменилось. Заметка —
`docs/superpowers/notes/2026-09-05-ochistitel-i-api-chankov.md`.

```java
getChunkLevel / getChunkLevelAt / getChunkResistance / getNight
cleanseChunk(уровень, чанкX, чанкZ, сила, приростСопротивления)
recordSnapshot(игрок, позиция)          // пустой крючок под подсистему 5
```

Правило 10.2 спека ядра («нельзя окопаться»: рядом с очагом 3+
очиститель держит чанк на 1, ниже не опускает) живёт **внутри
`cleanseChunk`**, а не в блоке очистителя — оно про баланс эпидемии,
и любой будущий очиститель обязан ему подчиняться.

`PlagueBridge` рефлексирует новые методы как **необязательные**:
у соседа старой версии отвалится одна способность, а не весь мод.

### Фермер — грядка бутона чумы

- `PlagueBloomCropBlock` — ванильная культура во всём, кроме скорости:
  растёт медленнее в `farmerBloomGrowthDivisor` раз (умолчание 2).
  Замедление сделано отсевом случайных тиков, а не своей формулой,
  чтобы не переписывать ванильный учёт света, воды и соседних грядок.
- **Бутон стал и сырьём, и семенем** (`PlagueBloomSeedItem`). Второго
  предмета нет намеренно: в лоре разницы нет. **Сажать может только
  Фермер** — гейт на посадке, не на крафте, тем же приёмом, что
  у отвара Клирика.
- **Дикий сбор** (`ClassPassives.дикийБутон`): заражённая трава
  `plaguecore` роняет бутон в чанке от уровня `farmerBloomWildMinLevel`
  с шансом `farmerBloomWildChance`. Открыт всем классам — эксклюзив
  Фермера это грядка, а не доступ к сырью. Блоки соседа опознаются
  по идентификатору строкой, не по типу.
- Текстура временная: ванильный нижний адский нарост, три стадии
  на восемь возрастов.

### Кузнец — андезитовый очиститель

Первый тир из спека ядра 10.1. `PurifierBlock` + `PurifierBlockEntity`,
без меню-контейнера: реагент кладётся правым кликом, состояние
читается правым кликом пустой рукой.

- **Ставить может кто угодно** — прямое требование спека, чтобы
  компания без Create не выпадала из защиты.
- **Класс входит через мастерство партии** (`ClassParty`), а не через
  право поставить блок: сила очистителя читает тир лучшего Кузнеца
  среди сейчас играющих. Нет Кузнеца — работает базовая сила, «уровень,
  доступный без класса» из спека классов 2.1. Обратно Кузнец получает
  мастерство за каждую удачную ночную очистку.
- **Питание — через рефлексию на Create** (`CreateBridge`), не
  наследованием от `KineticBlockEntity`. Наследование потребовало бы
  жёсткой зависимости, и без Create мод перестал бы грузиться целиком,
  вместе с Клириком и алтарём — ровно та беда, из-за которой Curios
  пришлось честно пометить `required`.
  **`ponytail:` стресс (SU) не потребляется** — читаем скорость
  соседа, но не нагружаем сеть, поэтому одно водяное колесо крутит
  сколько угодно очистителей и «64 SU на штуку» из спека не работает.
  Апгрейд — настоящее наследование, когда Очиститель переедет
  в `plaguecore` вместе с подсистемой 4.
- Работа привязана к **номеру ночи** из `plaguecore`, а не к счётчику
  тиков: переживает перезапуск и выгрузку чанка. Первая встреча с миром
  не отрабатывает задним числом за все прошедшие ночи.
- Реагент возвращается игроку при сломе блока.

### Летописец — точный уровень местности и снимок

- **Пассивка:** в панели Летописца появилась строка с точным уровнем
  заражения чанка, **на который он смотрит** (сервер делает `pick`
  на 48 блоков, не попал — берёт чанк под ногами).
  **Плагина Jade нет и не будет в этой версии:** он потребовал бы
  жёсткой зависимости на чужой API, а Jade в этом паке уже один раз
  ронял клиент (`2026-09-05-jade-otkachen-radi-zhazhdy`). Число то же,
  показывает своя панель.
- **Активка — снимок** (`ChroniclerSnapshot`). Щелчок камерой
  `exposure` открывает **всей партии** карту заражения вокруг точки
  съёмки на `chroniclerSnapshotMinutes`. Камера опознаётся по
  идентификатору предмета из конфига (`chroniclerCameraItems`) —
  жёсткой зависимости на `exposure` нет.
- Карта рисуется заливками в правом верхнем углу (`client.SnapshotHud`),
  свой чанк обведён белым. Ни модели, ни атласа, ни экрана.
- Упаковка сетки вынесена в чистый `SnapshotGrid` и покрыта тестами:
  байт в Java знаковый, и `0xFF` без маски читается как −1, а не как
  «нет данных» — это ровно тот класс ошибок, который находится глазами
  в игре, а не в логе.
- Вложение получило поле `снимокГотовТик`. В `CODEC` оно
  **необязательное** — иначе разбор старого сейва падал бы.

### Правка чисел прямо в игре

`/lmpcclasses tune` (право 2) печатает и меняет балансные числа
не выходя из игры, сразу записывая их в
`config/lmpc_classes-common.toml`. Заведено по прямой просьбе владельца
про скорость грядки; список не ограничен одним ключом, потому что все
эти числа подбираются одинаково — на живой сессии, по ощущению. Это
проектное правило «игровые числа наружу», доведённое до конца: баланс
должен править не пересборка мода и даже не перезапуск сервера.

### Проверка

`./gradlew test` — 16 тестов (11 старых + 5 на `SnapshotGrid`),
проходят. `./gradlew runServer` — регистрация проходит, сервер
поднимается (`Done`). Ошибки разбора рецептов в логе dev-сервера —
старые и ожидаемые: `plaguecore:spore_sac`, `plague_brew`
и `blighted_grass` в dev-окружении не существуют, мод там один.
**Живьём ничего из 0.7.0 не проверено** — это за владельцем.

## Устройство (0.6.1)

**Правки интерфейса по замечаниям с живого экрана.** Владелец запустил
игру, посмотрел алтарь и гримуар и назвал три дефекта; все три закрыты.

### Алтарь

- **Пометка «твой» наезжала на название класса.** Обе надписи рисовались
  на одной строке и считали ширину порознь. Теперь место под пометку
  резервируется до отрисовки имени, и обрезается имя, а не соседняя
  надпись.
- **Обрезанная роль читается при наведении.** У каждой строки списка
  теперь `Tooltip` с полной ролью и списком способностей — то, за чем
  раньше приходилось лезть в гримуар. Обрезка многоточием осталась
  (перенос сломал бы сетку списка), но больше ничего не прячет
  насовсем.
- Панель расширена с 280 до 320 пикселей.

### Гримуар

- **Отступы измерены, а не подобраны на глаз.** Границы бумаги внутри
  картинки разворота сняты замером самого файла: лист занимает тексели
  26–705 по горизонтали и 21–448 по вертикали, корешок сидит не по
  центру (365 из 368), снизу у книги толстая тень. Прежнее
  `ОТСТУП_НИЗ = 16` уводило текст на пять пикселей под деревянную
  обложку. Вся раскладка теперь считается из этих констант через
  `пX`/`пY` — тексель картинки в экранный пиксель.
- **Разворот тянется под окно** (300–460 пикселей вместо жёстких 360),
  ограничен и по ширине, и по высоте экрана. На мелком масштабе
  интерфейса книга больше и колонка текста шире; на крупном кнопка
  «Закрыть» перестала свисать за нижний край — она переехала внутрь
  книги, вниз левой страницы.
- **Правая страница прокручивается колесом.** Лор Клирика со всеми
  тремя способностями не влезал ни в какой разумный разворот, и старый
  код молча обрывал текст на полуслове первым `return` за нижним краем.
  Страница раскладывается в плоский список строк (`собратьСтраницу`)
  до отрисовки — только так у неё есть измеренная высота, к которой
  можно прижать прокрутку. Видимое окно режется `enableScissor`,
  на поле страницы рисуется тонкая закладка с положением. Заголовок
  и полоса мастерства прибиты к краям и не уезжают.

Автотестов на это нет: вся правка — арифметика раскладки внутри
`Screen`, вытащить её в чистую функцию дешевле не выходит. Проверяет
владелец глазами в игре.

## Устройство (0.6.0)

**Крупная правка: код, возможности, GUI.** Версия чинит один тихий
баг, из-за которого весь клиентский UI показывал неправду, доводит
мастерство до работающей механики и даёт трём пустым классам
по способности.

### Вложение наконец синкается — и это чинило больше, чем казалось

`PlayerClassData` получил `.sync(STREAM_CODEC)`. До этого вложение
жило только на сервере, а клиент читал свою пустую копию, поэтому:

- гримуар всегда открывался на Клирике (свой класс читался
  на клиенте и всегда был `NONE`);
- полоса мастерства всегда показывала 0, чем бы игрок ни играл;
- показать кулдаун смены заранее было нечем.

Синк идёт **всем, кто видит игрока**, а не только ему самому: так
Клирик под прицелом знает класс цели, не спрашивая сервер. Вложение
мутабельное и само себя не рассылает — после каждой правки поля
на сервере зовётся `PlayerClassData.синхронизировать(игрок)`.
Кодек сетевой пишется руками (четыре скаляра), а не через
`ByteBufCodecs.fromCodec` NBT-деревом.

### Мастерство стало механикой, а не полем

- `ClassMastery.java` — чистые функции тиров (1/2/3), порогов
  и множителей, без импортов Minecraft, проверяются `ClassMasteryTest`
  (шесть тестов). Пороги приходят из конфига, поэтому `тир()` терпит
  переставленные местами числа и не выдаёт третий тир на нуле.
- Мастерство **растёт** у всех четырёх классов, от профильного
  действия каждого: Клирик — за лечение (за союзника вдвое больше,
  чем за себя), Кузнец — за работу на наковальне, Фермер — за
  собранную созревшую культуру, Летописец — за минуту рядом
  с заражённым.
- Тир **что-то значит** у всех четверых, одним и тем же множителем
  (`masteryPowerPerTier`, `masteryCooldownCutPerTier`): у Клирика
  сильнее кулон и отвар и короче кулдаун, у Кузнеца чаще ремонт,
  у Фермера сытнее еда, у Летописца шире обзор.
- `/lmpcclasses mastery <кто> <число>` — админская выдача, иначе
  тиры 2 и 3 живьём не посмотреть.

### Три пустых класса получили по способности — `ClassPassives.java`

**Это честные заглушки, а не то, что обещает спек.** Верхние тиры
Очистителя, грядка `plague_bloom` и снимок-артефакт требуют Мира
и Лора, которых нет. Здесь у каждого стоит вандальная замена того же
настроения, целиком в конфиге, чтобы её было не жалко выкинуть.

- **Кузнец** — снаряжение в руках и на теле само чинится по очку
  раз в `smithRepairIntervalTicks`. Через `AnvilUpdateEvent` скидку
  на наковальне сделать нельзя: ванильная `AnvilMenu` перезаписывает
  цену после события, если событие не подменило и сам результат, —
  проверено по `CommonHooks.onAnvilChange`.
- **Фермер** — блюда сытнее на долю от собственной сытности блюда
  (`farmerFoodBonus`), а не плоской добавкой: иначе печенье кормило
  бы как стейк.
- **Летописец** — свой HUD `client/ChroniclerHud.java`: точная стадия
  и очки заражённости, свои и всех в радиусе. Плагина Jade, на который
  рассчитывал спек, в проекте нет (тот, что лежал в `plaguecore`, был
  заглушкой и удалён) — вместо интеграции с чужим модом ванильный
  `RenderGuiEvent`. Числа целиком приходят с сервера пакетом
  `ClassNetwork.Insight`: заражённость лежит в чужом вложении
  `plaguecore` и на клиент не синкается. Без `plaguecore` панель
  просто пуста.

Счётчиков «сколько тиков прошло» нет ни у кого — интервалы считаются
от мирового времени по модулю: переживают перезаход и не заводят
состояния, которое надо сохранять.

### GUI: обе страницы одной книги

Раньше гримуар выглядел рукописью, а алтарь — окном настроек из шести
серых ванильных кнопок. Общий язык вынесен в `client/ClassStyle.java`
(палитра, римские цифры, полоса мастерства с засечками); цвета классов
больше не лежат копией в каждом экране.

- **Алтарь переписан.** Пергамент вместо пустоты, строка на класс
  с цветной засечкой, названием и ролью, пометка «твой» и — главное —
  ответ на три вопроса заранее: какой у меня класс, какой тир, можно ли
  сменить прямо сейчас. Кулдаун виден **до** клика, а не вылезает
  ошибкой после; это стало возможно только с синком вложения.
- **Гримуар дополнен**: раздел «Что умеет» (книга про классы наконец
  говорит, что классы делают), строка «чем растёт мастерство», тир
  римской цифрой, засечки порогов на полосе, остаток до следующего
  тира, пометка своего класса в оглавлении, листание стрелками ← →.
  Разворот подрос с 320 до 360 пикселей — иначе новое не влезало.

### Тексты уехали в языковые файлы

Весь видимый текст — названия классов, роли, лор, способности,
сообщения команд, надписи экранов — теперь ключи перевода, а не
`Component.literal` в Java. Причина та же, по которой в проекте
вынесены наружу игровые числа: **лор не моя территория**, и править
его владельцу в `assets/lmpc_classes/lang/ru_ru.json` без пересборки
мода. Заодно `en_us.json` перестал врать — до этого он переводил
только названия предметов, а весь текст гримуара был русским в коде.
`ClassLore.java` стал публичным и отдаёт `Component`; мост
`ClassLoreAccess.java` за ненадобностью удалён.

### Починенные баги

- **Выбор своего же класса жёг кулдаун и срезал 70% мастерства.**
  Достаточно было ткнуть в свою строку в алтаре. Теперь это
  не «смена», а отказ с пояснением.
- **Отвар пропадал впустую на кулдауне.** Бутылка тратится только
  после того, как отвар подействовал, и только на сервере. У
  не-Клирика она всё так же пропадает — это цена попытки по спеку,
  а не баг.
- **Сервер молчал на каждый отказ напоить.** Игрок три секунды держал
  ПКМ и не получал ничего, включая объяснение. Теперь у каждой ветки
  отказа своя строка: не Клирик, далеко, нет отвара в руке, цель
  на кулдауне.
- **Админская выдача класса запирала игроку смену на полчаса.**
  `/lmpcclasses class` больше не ставит тик последней смены — смысл
  команды в том, чтобы быстро перебрать классы, а не наоборот.
- Неизвестное имя класса в сейве (чужая версия мода) больше не роняет
  разбор вложения, а читается как `NONE`.
- `ClassesApi.protectionBonus` больше не проверяет `Player instanceof
  LivingEntity` — это всегда истина.

### Проверка

`./gradlew test` — 11 тестов (5 старых + 6 новых), проходят.
`./gradlew runServer` — регистрация проходит, сервер поднимается.
Ошибки разбора рецептов в логе dev-сервера — старые и ожидаемые:
`plaguecore:spore_sac` и `plaguecore:blighted_grass` в dev-окружении
не существуют, мод там один. Живьём в игре механику проверяет
владелец.

## Устройство (0.5.2)

**Фон прозрачный, добавлено немного стиля.** Углы картинки (JPG не
хранит альфу) выкушены по порогу почти-чёрного в отдельный PNG-проход
(`Color.FromArgb(0,0,0,0)` на пикселях с R,G,B ≤ 16) — раньше на их
месте была сплошная чёрная плашка. Сверх этого — по просьбе владельца
«прояви творчество, не перегибая»:

- **Цветовой акцент на класс** (`цветКласса`) — тёплое золото Клирику,
  калёное железо Кузнецу, земляной зелёный Фермеру, лиловые чернила
  Летописцу (единственное место, где вообще есть лиловый — палитра
  проекта просит именно так). Красит заголовок, активную вкладку
  и полосу мастерства — раньше были отдельные несвязанные цвета.
- **Восковая печать** в углу правой страницы — заливка + буква,
  без новой текстуры.
- **Нумерация вкладок** (I–IV) слева, как оглавление настоящей книги.
- **Первое слово лора** — жирным и цветом класса, маленький кивок
  в сторону буквиц иллюминированных рукописей, без полноценной
  раскладки drop cap (риск ломки переноса строк не стоил эффекта).

## Устройство (0.5.1)

**Гримуар выглядит как разворот книги, не как панель с кнопками.**
Фон — картинка владельца, `textures/gui/codex_background.png`
(736×490, конвертирована из присланного JPG), рисуется через
`GuiGraphics.blit` с масштабом в позе — блит не умеет тянуть
источник и назначение раздельно, поэтому масштаб через
`pose().scale(...)`. Левая страница разворота — список четырёх
классов простым текстом (свой мини-виджет `ВкладкаКнопка extends
Button`, без ванильной серой кнопки — переопределён только
`renderWidget`, клики остались от `Button`), правая — заголовок,
роль, лор и, у своего класса, мастерство.

**Отступы внутри картинки подобраны на глаз**, не измерены точно —
я вижу только присланный кадр, не собственный рендер. Проверить
и поправить после первого взгляда в игре.

## Устройство (0.5.0)

**Гримуар** — личный экран «дневник призвания» (спек 2.1, 4–7).

- `ClassCodexItem.java` — предмет, ПКМ открывает клиентский экран.
  Крафта нет: выдаётся автоматически при первом `/lmpcclasses choose`
  (`ClassCommands.выдатьГримуарЕслиНадо` — проверка по инвентарю, не
  по флагу, поэтому и потерянный гримуар восполнится при следующей
  смене класса).
- `client/ClassCodexScreen.java` — свой `Screen`, не ванильная книга.
  Мастерство (полоса 0–100 + число) рисуется только на вкладке
  твоего класса — у чужих вкладок только роль и лор, чтобы не путать
  «характеристику» с «энциклопедией».
- `ClassLore.java` (package-private) + `ClassLoreAccess.java`
  (публичный мост в `client`) — заголовок/роль/лор по каждому классу.
  **Лор — черновик**, не финальный текст: по договору проекта лор не
  моя территория, писать/править — владельцу.
- Тиров мастерства в коде ещё нет (ничего его не растит) — полоса
  на экране рисуется как обычная шкала 0–100, без деления, чтобы
  не выдумывать пороги заранее.

## Устройство (0.4.4)

**Кулон не надевался — забытый файл, не баг предмета.** Curios мало
объявить тег (`data/curios/tags/item/necklace.json`, был с самого
начала) — слот ещё нужно **выдать сущности** отдельным файлом,
Curios сам по себе не даёт игроку ни одного слота. Добавлен
`data/lmpc_classes/curios/entities/player.json`:
`{"entities": ["minecraft:player"], "slots": ["necklace"]}` —
тот же формат, что у `supplementaries`/`sophisticatedbackpacks`
в паке (сверено с их джарниками). Лог `Curios API: Loaded N curio
entities` не показатель — там считаются entity-типы, а не файлы,
несколько модов дописывают слоты одному и тому же `player` в одну
запись.

## Устройство (0.4.3)

**Обводка решена — x-ray-силуэт, вернулись к первой версии 0.4.0.**
Миксин признан слишком рискованным (см. ниже) ради декоративной
детали; дистанция вплотную (1.5 блока) делает просвечивание сквозь
стены не проблемой на практике. `Entity.setSharedFlag(6, …)` через
рефлексию — **видно только Клирику, который целится**: правится
локальная копия сущности на этом клиенте, ни сервер, ни другие
игроки об этом не узнают (в отличие от `setGlowingTag`, который
рассчитан на серверный синк всем и для чисто клиентского эффекта
не годится — уже разбиралось в 0.4.0).

## Устройство (0.4.2)

`ClericsBrewItem.interactLivingEntity` перехватывает клик по игроку
и возвращает `CONSUME` — без этого ваниль считала клик по сущности
промахом и откатывалась на обычное использование предмета: Клирик,
целящийся в союзника с зажатым ПКМ, выпивал бутылку сам, и играла
анимация питья у него. Правка одной строки решила оба симптома разом.

**Обводка — открытый технический вопрос.** Настоящая обводка «по
модельке, по скину» (как у зелья свечения) требует либо миксина
в рендер сущности (`LivingEntityRenderer.render` не даёт точки
расширения для второй, увеличенной и тонированной отрисовки той же
модели через публичный API — поза пересчитывается и схлопывается
внутри одного вызова), либо смириться с тем, что «по модельке» и
«без просвечивания сквозь стены» одновременно без миксина не
достать. Проект избегает миксинов (`lmpc_shade` — осознанно, «на
ванильных хуках»). Сейчас стоит `renderLineBox` — коробка, честно
проверяется на глубину, но не силуэт. Ждёт решения владельца:
коробка и дальше, x-ray-силуэт (как зелье свечения, первая версия
0.4.0), или заводить миксин.

## Устройство (0.4.0)

**Напоить союзника наведением** — второй способ применить
`clerics_brew`, кроме питья себя. Не магия на расстоянии: Клирик
должен стоять вплотную к цели (`clericFeedMaxDistance`, умолчание
1.5 блока — «половина от блока», как просил владелец), обводка
не должна светиться сквозь стены.

- `client/BrewTargeting.java` — держишь отвар в руке, стоишь рядом
  и смотришь на игрока: его модель обводится тонкой линией
  (`RenderHighlightEvent.Entity` + `LevelRenderer.renderLineBox`,
  тем же приёмом, что ваниль рисует рамку на блоке под прицелом) —
  **проверяется на глубину**, через стену не видно. Первый черновик
  обводил через `Entity`-флаг свечения (эффект зелья свечения,
  специально рассчитанный светиться сквозь стены) — переделано
  на честную линию по прямой просьбе владельца, не ушло в коммит
  в старом виде. Над головой всплывает «⚕ ПКМ — напоить»
  (`RenderNameTagEvent`, `setCanRender(TRUE)`, форсит нетэг даже если
  у игрока он выключен). Держишь ПКМ — вместо надписи растёт текстовый
  прогресс-бар (`█████░░░░░`, 10 сегментов). Цель сдвинулась больше
  чем на пол-блока от точки, где начал жать, — прогресс сгорает
  и отсчёт стартует заново. Дошёл до конца (`clericFeedChannelTicks`,
  умолчание 60 тиков) — клиент шлёт `ClassNetwork.FeedRequest`,
  дальше решает сервер.
- `ClassNetwork.java` — сервер не доверяет клиенту: заново проверяет
  класс отправителя, дистанцию (`clericFeedMaxDistance`), что отвар
  физически в руке, и кулдаун **цели** (`отварГотовТик` — тот же
  счётчик, что у самостоятельного питья, только теперь про того,
  кого лечат,
  а не кто лечит). Тратит отвар из руки Клирика, лечит цель тем же
  `PlagueBridge.cure/grantImmunity`, что и self-drink.

**Решение, которое стоит знать:** «должен стоять на месте» я понял
как требование к цели (тому, кого поят), не к Клирику — Клирик может
двигаться и целиться свободно, канал рвёт только движение того,
кого лечат. Если задумывалось наоборот — переставить проверку
на позицию самого Клирика, это одна строка в `BrewTargeting.тик`.

## Устройство (0.3.0)

**Клирик — первый класс с реальными способностями.**

- `ClericsPendantItem.java` — кулон, Curios-предмет (слот `necklace`,
  тег `data/curios/tags/item/necklace.json`), сам ничего не делает —
  `ICurioItem` без единого переопределения. Носить может кто угодно,
  защита реальная только у Клирика (`ClassesApi.protectionBonus`,
  раздел ниже). Крафт: золотой слиток + нить + `plaguecore:spore_sac`.
- `ClericsBrewItem.java` — улучшенный отвар. В отличие от обычного
  `plaguecore:plague_brew` работает на любой стадии, но гейт —
  **на использовании, не на крафте**: рецепт (`plague_brew` +
  `spore_sac` + золотая морковка) доступен всем, у не-Клирика бутылка
  просто пропадает без эффекта. Свой кулдаун на игрока
  (`PlayerClassData.отварГотовТик`, `clericBrewCooldownMinutes`).
- `PlagueBridge.java` — мост К `plaguecore`: рефлексия на
  `dev.denthe.plaguecore.mc.PlagueApi.cure/grantImmunity` (раздел
  ниже, «Мост к `plaguecore`»).
- `ClassesApi.java` — мост ОТ `plaguecore`: `protectionBonus(Player)`,
  которую `plaguecore` зовёт рефлексией из `ClassBridge`. Полная
  защита (`clericPendantProtection`, 0.15 по умолчанию) — Клирику
  с кулоном, половина (`clericPendantOtherClassFraction`) — всем
  прочим классам с тем же кулоном, ноль — без кулона.
- **Реагент (раздел 8 спека), пока без класс-эксклюзива.**
  `plague_bloom` — сырой бутон, крафтится из `blighted_grass` × 3 +
  `spore_sac` (заглушка вместо дикого куста в Гнили — ни Фермера, ни
  сбора в мире ещё нет). `cleansing_agent` — обработанный реагент,
  из двух бутонов + стеклянная бутылка. **Крафт открыт всем** —
  в отличие от отвара, гейта по классу нет: пока ни один потребитель
  (Очиститель, курильница) не спрашивает, кто его сварил, вводить
  проверку рано. Появится реальное эксклюзивное потребление —
  гейтить тем же приёмом, что `clerics_brew`.
- `ClassCreativeTab.java` — своя вкладка «Классы» в творческом
  инвентаре (как `PlagueCreativeTab` у `plaguecore`): тянет всё
  из `ClassBlocks.ПРЕДМЕТЫ` и `ClassItems.ПРЕДМЕТЫ` реестров разом,
  новый предмет добавлять сюда не придётся.

## Устройство (0.2.0)

- `LmpcClasses.java` — точка входа, регистрирует всё ниже.
- `ClassSwitch.java` — чистая функция кулдауна смены класса, без
  импортов Minecraft, проверяется обычным JUnit (`ClassSwitchTest`).
- `PlayerClassData.java` — Data Attachment на игроке: текущий класс
  (`enum Класс`: NONE/CLERIC/SMITH/FARMER/CHRONICLER), тик последней
  смены и `мастерство` (раздел 2.1 спека). `сменитьКласс(...)` — смена
  со срезом мастерства старого класса до `masteryKeepFraction`, а не
  обнулением. `copyOnDeath` — смерть класс и мастерство не сбрасывает.
- `ClassAltarBlock.java` + `ClassBlocks.java` — единственный блок
  мода, «Алтарь призвания». Правый клик без предмета в руке
  (`useWithoutItem`) открывает клиентский экран, никакого меню-
  контейнера нет — путёвок-предметов больше не существует (крафт
  под них требовал `cleansing_agent` из `plaguecore`, которого ещё
  нет; блок эту зависимость снял целиком). Дроп — сам блок
  (`data/lmpc_classes/loot_table/blocks/class_altar.json`).
- `client/ClassAltarScreen.java` — пять кнопок (без класса + четыре
  класса), каждая шлёт `/lmpcclasses choose <класс>`
  (`Minecraft.getConnection().sendCommand`) тем же приёмом, что панель
  `lmpc_gmtools`. Экран ничего не решает — сервер сам проверяет
  кулдаун и режет мастерство.
- `ClassCommands.java` — `/lmpcclasses choose <класс>` (себе, право 0,
  настоящий путь смены) и `/lmpcclasses class <игрок> [класс]`
  (админский обход кулдауна и среза мастерства, право 2, для проверки
  на живом сервере).
- `ClassesConfig.java` — `config/lmpc_classes-common.toml`,
  `classSwitchCooldownMinutes` (умолчание 30), `masteryKeepFraction`
  (умолчание 0.3).

**Текстуры блока нет** — модель ссылается на ванильный
`minecraft:block/chiseled_stone_bricks` как временную заглушку,
не на свою картинку. Заменить, когда появится своя, в
`models/block/class_altar.json`.

## Рабочий процесс (требования владельца, как у gmtools/shade)

- **Версия за каждое весомое изменение:** поднять `mod_version` в
  `gradle.properties` (`0.МИНОР.0` фичи, `0.МИНОР.ПАТЧ` фиксы).
- **Jar нужен и в `mods/` сервера**, версии клиента и сервера должны
  совпадать. `mods/*.jar` в `.gitignore`.
- Проверка: `./gradlew runServer` — поднять dev-сервер, убедиться, что
  регистрация проходит без ошибок, затем убить java-процесс. Живьём
  в игре механику проверяет владелец.
- Сборочное окружение: `GRADLE_USER_HOME=D:\dev-cache\gradle`, там в
  `gradle.properties` — `org.gradle.java.home=D:/JDK21`.
- Git — полная автономия.

## Что дальше

Полная ревизия технического долга — `docs/superpowers/notes/
2026-09-07-lmpc-classes-tehnicheskij-dolg.md`. Коротко:

- **Электрический тир Очистителя** (спек ядра 10.1): питание от FE
  через `createaddition`, 5 × 5 чанков, 8 реагентов за ночь. Оправу
  по курсу create-style брать готовую у `createaddition`.
- **Своя строка в очках инженера** (`IHaveGoggleInformation`): остаток
  реагента, тир Кузнеца, хватает ли скорости. Стресс и скорость
  `KineticBlockEntity` показывает и так.
- **Спек 10.1 разошёлся со сборкой** — радиусы, стресс и «без передач».
  Развилка за владельцем, варианты в заметке.
- **Класс-эксклюзив Клирика на обработку реагента** (спек классов 8):
  потребитель наконец появился, вопрос можно поднимать.
- **Мёртвая ручка `smithOverdriveItems`** и два долга `ponytail:`
  в `PurifierBlockEntity` (отчёт живёт до перезапуска, реагент берётся
  плоско за ночь, а не за чанк).
- Настоящий лор-артефакт из снимка (подсистема 5): крючок
  `PlagueApi.recordSnapshot` дёргается на каждый снимок и пуст.
- Мастерство партии для построек (спек классов 2.1): концепция есть,
  реализации нет; `ClassesApi.mastery/masteryTier` наружу уже отданы.
- Свои текстуры алтаря, кулона, отвара, бутона и реагента (владелец,
  `textures_src/`) вместо заглушек на ванильных.
- **Живая проверка** — за владельцем: вал в рендере, нагрузка в очках,
  встали ли уже стоящие в мире очистители (им нужен вертикальный привод
  снизу), крафты обоих тиров, курильница из 0.12.0.

Связано: `docs/superpowers/specs/2026-09-04-klassy-design.md`,
lmpc-gmtools-sostoyanie, lmpc-shade-sostoyanie (память Kuragane).
