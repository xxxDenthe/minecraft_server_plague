# Как генерировать органические Minecraft-модели через Blockbench MCP

Методичка. Написана по итогам реальной сборки модели **гнилого сердца**
(`rotten_heart/`) — 297 кубов, 23 кости, анимация биения, 7 текстур в атласе.

Всё, что здесь есть, проверено на этой модели. Раздел про зомби/корову — это
применение того же метода к другим формам, код там по аналогии.

Документ можно отдать другому Claude Code как рабочую инструкцию.

---

## 0. Главный принцип

**Не расставляй кубы руками. Пиши генератор.**

Органическая форма (сердце, опухоль, мутировавшая туша) — это сотни кубов.
Расставлять их по одному через MCP-вызовы — это сотни round-trip'ов, часы работы
и результат, который стыдно показать. Вместо этого:

> Один вызов `risky_eval` с JavaScript-скриптом, который строит всю форму
> из математики прямо внутри Blockbench.

Скрипт видит `Cube`, `Group`, `Texture`, `Animation` — весь API Blockbench.
Генерация 300 кубов занимает один вызов и полсекунды.

Второй принцип, не менее важный:

> **Смотри на результат глазами после каждого шага.**

`capture_screenshot` после каждой генерации. Половина ошибок (рассыпавшиеся корни,
слипшиеся ветки, невидимая текстура) видна только на картинке, а в цифрах выглядит
нормально.

---

## 1. Порядок работы

Порядок не случайный. Каждый шаг ломается, если сделать его раньше времени.

| # | Шаг | Почему именно здесь |
|---|---|---|
| 1 | Выбрать формат | Формат решает, что вообще возможно. Менять потом — больно. |
| 2 | Сгенерировать основную массу | Силуэт важнее деталей. Сначала проверь, что форма читается. |
| 3 | Добавить отростки/конечности | Отдельным проходом, отдельными группами. |
| 4 | Сделать текстуры + починить UV | UV **всегда** надо считать вручную, см. ловушку 5.1. |
| 5 | Нарезать на кости | До анимации. Кости-дети не мешают анимации на родителях. |
| 6 | Анимация | Только после переключения в режим `animate`. |
| 7 | Атлас + конверт формата + экспорт | Атлас строить **до** конвертации, см. ловушку 5.4. |

---

## 2. Выбор формата — таблица решений

Первый и самый дорогой выбор. Спроси пользователя, не угадывай.

| Формат | Анимация | Много текстур | Свободный поворот | Когда брать |
|---|---|---|---|---|
| `java_block` | нет | да | нет (только 22.5°/45°, одна ось) | статичный блок в ванильной игре |
| `free` (Generic) | да | да | да | черновик, промежуточный этап |
| `bedrock` (Bedrock Entity) | да | **нет, одна** | да | **GeckoLib для Java-модов** ← основной выбор |
| `modded_entity` | да | **нет, одна** | да | генерация Java-класса модели |

**Что надо честно сказать пользователю сразу:**

- `java_block` физически не умеет анимацию. Это ограничение Minecraft, не Blockbench.
  Модель блока в Java — статичный JSON.
- В `java_block` координаты обязаны лежать в диапазоне **−16 … 32**. Это 48 юнитов,
  ровно 3×3×3 блока. Больше нельзя, игра отбросит модель.
- Формат `bedrock` в Blockbench — это **не Bedrock Edition**. Это формат файла
  `.geo.json`, который использует GeckoLib для **Java**-модов. Название путает,
  надо объяснить.
- `bedrock` и `modded_entity` держат **ровно одну текстуру**. Если у тебя их семь —
  склеивай в атлас (раздел 4.4).

Проверить, что умеет формат:

```js
Object.keys(Formats).map(k => {
  const F = Formats[k];
  return `${k} anim:${F.animation_mode?1:0} single_tex:${F.single_texture?1:0} box_uv:${F.box_uv?1:0}`;
}).join(' | ')
```

---

## 3. Ядро метода — четыре функции

### 3.1. Шум по позиции

Нужен везде: неровность поверхности, выбор текстуры, разброс размеров.
Детерминированный — одна и та же точка всегда даёт одно число.

```js
function h(a, b, c) {
  const s = Math.sin(a * 12.9898 + b * 78.233 + c * 37.719) * 43758.5453;
  return s - Math.floor(s);   // 0..1
}
```

Высокочастотный. Если нужны **плавные пятна** (для выбора текстуры), бери другой —
сумму синусов:

```js
function smooth(x, y, z) {
  return Math.sin(x*0.21 + 1.3) * Math.cos(y*0.17 - 0.7)
       + Math.sin(z*0.19 + 2.1) * Math.cos(x*0.13 + 0.4)
       + Math.sin(y*0.29 - 1.1) * Math.cos(z*0.23 + 1.7);
}   // примерно -3..3
```

### 3.2. Форма как функция расстояния (SDF)

Пишешь функцию `f(x,y,z)`, которая возвращает отрицательное число внутри формы
и положительное снаружи. Объединение фигур — это `Math.min`.

Сердце = две сферы сверху + конус, сужающийся вниз:

```js
const cx = 8, cy = 3, cz = 8, R = 16;   // центр и радиус

function f(x, y, z) {
  const u = (x-cx)/R, v = (y-cy)/R, w = (z-cz)/R;   // нормализуем в -1..1

  // левая доля
  const d1 = Math.sqrt((u+0.42)**2 + (v-0.38)**2 + w*w) - 0.63;
  // правая доля
  const d2 = Math.sqrt((u-0.42)**2 + (v-0.38)**2 + w*w) - 0.63;
  // конус вниз к острию
  let d3 = 9;
  if (v <= 0.4 && v >= -1.05) {
    d3 = Math.sqrt(u*u + w*w) - 0.9 * ((v + 1.05) / 1.45);
  }

  // шум по поверхности — вот от этого форма выглядит гнилой, а не пластиковой
  return Math.min(d1, d2, d3) + (h(x,y,z) - 0.5) * 0.10;
}
```

Амплитуда шума `0.10` — это подобранное значение. Меньше — форма слишком гладкая
и «резиновая». Больше — куски начинают отваливаться в воздух.

**Кубик деталей для других форм:**

| Нужно | Формула |
|---|---|
| Сфера | `Math.sqrt(u²+v²+w²) - r` |
| Эллипсоид | `Math.sqrt((u/a)²+(v/b)²+(w/c)²) - 1` |
| Капсула / конечность | расстояние до отрезка минус радиус |
| Конус | `Math.sqrt(u²+w²) - r * ((v - v0) / len)` |
| Объединить | `Math.min(a, b)` |
| Вычесть (дыра) | `Math.max(a, -b)` |
| Пересечь | `Math.max(a, b)` |

### 3.3. Оболочка — не заливай форму целиком

Полный объём — это тысячи кубов, из которых видно процентов десять.
Берём только те ячейки, у которых есть сосед снаружи.

```js
const S = 4;            // размер ячейки в юнитах
const CL = v => Math.max(-16, Math.min(32, v));   // зажим для java_block

const g = new Group({ name: 'heart_body', origin: [cx, cy, cz] });
g.init();

let n = 0;
for (let x = cx-R-2; x <= cx+R+2; x += S)
for (let y = cy-R-2; y <= cy+R+2; y += S)
for (let z = cz-R-2; z <= cz+R+2; z += S) {

  if (f(x,y,z) >= 0) continue;                    // снаружи — пропускаем

  const shell = f(x+S,y,z)>=0 || f(x-S,y,z)>=0    // есть сосед снаружи?
             || f(x,y+S,z)>=0 || f(x,y-S,z)>=0
             || f(x,y,z+S)>=0 || f(x,y,z-S)>=0;
  if (!shell) continue;                           // внутри — не нужен

  // разброс размера и позиции — от этого поверхность выглядит рваной
  const r  = S*0.5 + 0.5 + h(x*3, y*3, z*3) * 1.1;
  const ox = (h(x, y*2, z) - 0.5) * 1.2;
  const oy = (h(x, y, z*2) - 0.5) * 1.2;
  const oz = (h(x*2, y, z) - 0.5) * 1.2;

  const c = new Cube({
    name: 'flesh_' + n, autouv: 1,
    from: [CL(x+ox-r), CL(y+oy-r), CL(z+oz-r)],
    to:   [CL(x+ox+r), CL(y+oy+r), CL(z+oz+r)]
  });
  c.addTo(g); c.init(); n++;
}
```

**Порядок вызовов у Blockbench жёсткий:** `new Cube({...})` → `.addTo(group)` → `.init()`.
Если сделать `.init()` до `.addTo()`, куб улетит в корень аутлайнера.

**Как подбирать `S` (размер ячейки):**

| S | Кубов на форму диаметром ~32 | Вид |
|---|---|---|
| 3 | ~600 | детально, но игра будет тормозить |
| 4 | ~105 | **рабочий вариант**, крупная органика |
| 5 | ~70 | грубо, для мелких объектов |
| 6 | ~45 | совсем кубично |

### 3.4. Отростки — рекурсивный рост

Корни, вены, ветки, щупальца, обломки костей. Одна функция на всё.

```js
let m = 0;

function grow(group, prefix, p, d, steps, th, dec, wob, depth, bias, branchChance) {
  for (let i = 0; i < steps; i++) {

    // виляние + постоянный уклон (bias: вверх для веток, вниз для корней)
    d = [ d[0] + (Math.random()-0.5)*wob,
          d[1] + (Math.random()-0.5)*wob + bias,
          d[2] + (Math.random()-0.5)*wob ];

    const L = Math.hypot(d[0], d[1], d[2]);
    if (L < 0.001) break;
    d = [d[0]/L, d[1]/L, d[2]/L];

    // ВАЖНО: шаг МЕНЬШЕ толщины, иначе кубы не соприкоснутся
    const st = Math.max(0.9, th * 0.62);
    p = [p[0]+d[0]*st, p[1]+d[1]*st, p[2]+d[2]*st];

    // вышли за границы — обрываем ветку
    if (p[0]<-15||p[0]>31||p[1]<-15||p[1]>31||p[2]<-15||p[2]>31) break;

    const r = th / 2;
    const c = new Cube({
      name: prefix + '_' + m, autouv: 1,
      from: [CL(p[0]-r), CL(p[1]-r), CL(p[2]-r)],
      to:   [CL(p[0]+r), CL(p[1]+r), CL(p[2]+r)]
    });
    c.addTo(group); c.init(); m++;

    th = Math.max(1.2, th * dec);        // сужаемся

    // ветвление
    if (depth < 1 && i > 3 && Math.random() < branchChance) {
      grow(group, prefix, [...p],
           [d[0]+(Math.random()-0.5)*1.4, d[1]*0.6, d[2]+(Math.random()-0.5)*1.4],
           Math.max(3, steps-i-4), th*0.65, dec, wob*1.5, depth+1, bias*0.5, branchChance);
    }
  }
}
```

**Самая частая ошибка:** шаг больше толщины. Тогда вместо ветки получается
пунктирная линия из отдельных кубиков. Держи `st ≈ th * 0.6`.

Найти точки старта на поверхности — марш из центра наружу до смены знака SDF:

```js
for (let k = 0; k < 11; k++) {
  const a  = Math.random()*Math.PI*2;
  const ph = Math.acos(1 - 2*Math.random());       // равномерно по сфере
  let d = [Math.sin(ph)*Math.cos(a), Math.cos(ph)*0.8 - 0.3, Math.sin(ph)*Math.sin(a)];
  const L = Math.hypot(...d); d = d.map(v => v/L);

  let t = 2;
  while (t < 30 && f(cx+d[0]*t, cy+d[1]*t, cz+d[2]*t) < 0) t += 1;

  // старт чуть ВНУТРИ тела, чтобы отросток врастал, а не висел рядом
  const p0 = [cx+d[0]*(t-3), cy+d[1]*(t-3), cz+d[2]*(t-3)];
  grow(rootsGroup, 'root', p0, d, 9 + (Math.random()*4|0), 2.8, 0.9, 0.5, 0, -0.06, 0.14);
}
```

`Math.acos(1 - 2*rnd)` — а не просто `rnd*PI`. Иначе точки скучатся у полюсов.

**Параметры для разных типов отростков:**

| Тип | th | dec | wob | bias | branch |
|---|---|---|---|---|---|
| Толстые артерии вверх | 6 | 0.90 | 0.28 | +0.10 | 0.18 |
| Корни-заражение вниз | 2.8 | 0.90 | 0.50 | −0.06 | 0.14 |
| Тонкие вены по поверхности | 1.8 | 0.95 | 0.70 | 0 | 0.25 |
| Обломки костей (прямые) | 3 | 0.85 | 0.08 | 0 | 0.05 |

---

## 4. Текстуры

### 4.1. Не одна текстура, а семья

Одна текстура на всю модель = скучно, глаз не за что зацепить.
Пять текстур **одной палитры** = живая гнилая поверхность.

Гармония держится на трёх правилах:

1. **Одна база.** Все от тёплого серого. Меняется только яркость.
2. **Акценты слабые.** Красное, зелёное — только `alpha 0.06…0.26`. Как только
   акцент становится ярким, семья рассыпается на разноцветные заплатки.
3. **Раскидывать плавно, а не случайно.** См. 4.3.

```js
function mk(o) {
  const W = 32;
  const cv = document.createElement('canvas'); cv.width = W; cv.height = W;
  const x = cv.getContext('2d');

  x.fillStyle = o.base; x.fillRect(0, 0, W, W);

  // мягкая крапчатость: полупрозрачные прямоугольнички 1-3px
  for (let i = 0; i < 170; i++) {
    const up = Math.random() < 0.5;
    const a = (Math.random() * o.spread / 255 * 0.45).toFixed(3);
    x.fillStyle = up ? `rgba(255,250,242,${a})` : `rgba(0,0,0,${a})`;
    x.fillRect(Math.random()*W|0, Math.random()*W|0,
               1+(Math.random()*3|0), 1+(Math.random()*3|0));
  }

  // цветные акценты: [ 'r,g,b', сколько, разброс alpha ]
  (o.spots || []).forEach(sp => {
    for (let j = 0; j < sp[1]; j++) {
      x.fillStyle = `rgba(${sp[0]},${(0.06 + Math.random()*sp[2]).toFixed(3)})`;
      x.fillRect(Math.random()*W|0, Math.random()*W|0,
                 1+(Math.random()*3|0), 1+(Math.random()*3|0));
    }
  });

  // трещины — ломаные, не прямые
  x.lineWidth = 1;
  x.strokeStyle = `rgba(0,0,0,${o.crackA})`;
  for (let k = 0; k < o.cracks; k++) {
    x.beginPath();
    let px = Math.random()*W, py = Math.random()*W;
    x.moveTo(px, py);
    for (let s = 0; s < 4; s++) {
      px += (Math.random()-0.5)*11; py += (Math.random()-0.5)*11;
      x.lineTo(px, py);
    }
    x.stroke();
  }

  return new Texture({ name: o.name }).fromDataURL(cv.toDataURL()).add();
}
```

Семья, которая сработала на сердце:

```js
const flesh = [
  mk({name:'flesh_1_necrotic', base:'#2b2825', spread:70,  cracks:14, crackA:0.55, spots:[['84,46,40',18,0.20]]}),
  mk({name:'flesh_2_rot',      base:'#3a3632', spread:92,  cracks:10, crackA:0.42, spots:[['92,54,48',24,0.22],['70,76,60',12,0.14]]}),
  mk({name:'flesh_3_mould',    base:'#403f37', spread:88,  cracks:8,  crackA:0.35, spots:[['74,84,62',34,0.26],['92,54,48',10,0.14]]}),
  mk({name:'flesh_4_dry',      base:'#4b4640', spread:100, cracks:9,  crackA:0.38, fibers:10, fiberA:0.28, spots:[['96,60,50',14,0.16]]}),
  mk({name:'flesh_5_scar',     base:'#565049', spread:104, cracks:7,  crackA:0.30, fibers:14, fiberA:0.40, spots:[['104,62,52',20,0.20]]}),
];
```

Обрати внимание: от тёмной к светлой база идёт ровной лесенкой
`#2b2825 → #3a3632 → #403f37 → #4b4640 → #565049`, а трещин становится
меньше. Это и создаёт ощущение «здесь сгнило сильнее, а здесь ещё держится».

### 4.2. Пиксельный шум ≠ текстура

Первая попытка была `createImageData` + рандом на каждый пиксель. Выглядело как
дамасская сталь, а не как плоть. Полупрозрачные **пятна 1-3px поверх базы**
дают мягкость, которую попиксельный рандом не даёт никогда.

### 4.3. Раскидывать текстуры пятнами, а не рандомом

Случайное распределение = шахматная доска, читается как баг.
Берём плавный шум + высоту, потом **ранжируем**, чтобы каждой текстуры досталось
поровну.

```js
const list = [];
bodyGroup.forEachChild(e => {
  if (!(e instanceof Cube)) return;
  const my = (e.from[1] + e.to[1]) / 2;
  const mx = (e.from[0] + e.to[0]) / 2;
  const mz = (e.from[2] + e.to[2]) / 2;

  const n  = (smooth(mx, my, mz) + 3) / 6;                        // 0..1 плавно
  const yn = Math.max(0, Math.min(1, (my - (cy-R*1.05)) / (R*1.45)));

  list.push({ e, s: 0.66*n + 0.34*yn });   // 2/3 пятна, 1/3 «низ гнилее»
}, Cube);

// ранжирование — гарантирует ровное распределение по 5 текстурам
list.sort((a, b) => a.s - b.s);
list.forEach((item, i) => {
  const t = flesh[Math.min(4, Math.floor(i / list.length * 5))];
  for (const k in item.e.faces) item.e.faces[k].texture = t.uuid;
});
```

Без ранжирования получилось `[0, 2, 42, 53, 8]` — две текстуры из пяти просто
не использовались. С ранжированием — `[21, 21, 21, 21, 21]`.

### 4.4. Атлас — склеить семью в одну картинку

Нужно, когда формат держит одну текстуру (`bedrock`, `modded_entity`).
Плитки 32×32, сетка 4 в ряд:

```js
const T = 32, COLS = 4;
const order = ['flesh_1_necrotic', ..., 'root_bark'];
const AW = COLS*T, AH = Math.ceil(order.length/COLS)*T;   // 128 x 64

const cv = document.createElement('canvas');
cv.width = AW; cv.height = AH;
const ctx = cv.getContext('2d');
ctx.imageSmoothingEnabled = false;        // иначе плитки размажутся по швам

order.forEach((name, i) => {
  const t = Texture.all.find(x => x.name === name);
  ctx.drawImage(t.img, (i%COLS)*T, Math.floor(i/COLS)*T, T, T);
});
```

Пересчёт UV — сдвинуть координаты в свою плитку, не вылезая за её край:

```js
function remap(a, b, off) {
  const s0 = Math.min(a,b), s1 = Math.max(a,b), w = s1 - s0;
  let n0 = ((s0 % T) + T) % T;      // заворачиваем в 0..T
  if (n0 + w > T) n0 = T - w;       // прижимаем, чтобы не залезть в соседа
  if (n0 < 0) n0 = 0;
  return (a <= b) ? [n0+off, n0+w+off] : [n0+w+off, n0+off];   // зеркальные грани
}
```

Тернарник в конце — для зеркальных граней (`up`/`down` часто идут задом наперёд).
Без него верх и низ модели становятся мятыми.

---

## 5. Ловушки, на которых я реально споткнулся

Это не теория. Каждая из них стоила отдельного захода.

### 5.1. UV не считаются сами — текстура будет одноцветной

`autouv: 1` в конструкторе куба **ничего не делает**. Грани остаются с
`uv: [0,0,1,1]` — это один пиксель, растянутый на всю грань. Модель выглядит
залитой сплошным цветом, и кажется, что текстура не применилась.

```js
cube.autouv = 1;
cube.mapAutoUV();          // ← вот это обязательно, вручную, для каждого куба
```

Проверить:

```js
Cube.all[0].faces.north.uv     // [0,0,1,1] = сломано; [0,0,6.8,6.8] = ок
```

### 5.2. Ветки рассыпаются в пунктир

Шаг вдоль ветки должен быть **меньше** толщины куба. `st = th * 0.6`.
При `st = th * 1.1` кубы не касаются, и получается цепочка отдельных кубиков.

### 5.3. Конвертация формата сдвигает координаты

При переходе из формата без костей (`java_block`, `bone_rig: false`) в формат с
костями (`free`, `bedrock`) координаты кубов становятся **относительно origin
родительской группы**.

Последствие: любые «право/лево/верх/низ», посчитанные от старого центра, поедут.
У меня нарезка на куски дала `48 / 24 / 9 / 2 / 22` вместо ровных долей.

**Лечение:** после конвертации не полагайся на известный центр. Считай медиану
по факту (см. 6.1).

### 5.4. Атлас строить ДО конвертации формата

`Formats.bedrock.convertTo()` выкидывает лишние текстуры, потому что формат держит
одну. Если конвертировать раньше, чем склеен атлас — исходники уже потеряны.

Правильный порядок в одном скрипте:

```
1. собрать атлас из Texture.all      (пока их ещё 7)
2. посчитать и запомнить новые UV
3. удалить старые текстуры, добавить атлас
4. Formats.bedrock.convertTo()
5. Project.box_uv = false; texture_width/height = 128/64
6. заново разложить сохранённые UV   ← конвертация их портит, восстанавливаем
```

Шаг 6 обязателен: `convertTo` пытается пересчитать UV в box-формат.

### 5.5. Keyframes падают вне режима animate

```js
new Animation({name:'beat', loop:'loop', length:1.2}).add();
animator.createKeyframe(...);
// TypeError: Cannot read properties of null (reading 'setLength')
```

`Timeline` не существует, пока не открыт режим анимации. Сначала:

```js
Modes.options.animate.select();
anim.select();
// теперь можно createKeyframe
```

### 5.6. Записать PNG из Blockbench

`Blockbench.writeFile` со `savetype: 'image'` принимает **data-URL**, а не голый
base64. Иначе он попробует скопировать файл по пути, равному base64-строке.

```js
Blockbench.writeFile(path, {
  content: 'data:image/png;base64,' + tex.getBase64(),
  savetype: 'image'
});
```

### 5.7. Модуль `fs` недоступен

В контексте `risky_eval` глобального `fs` нет. Папки создавать нечем — либо пиши
в существующую, либо создавай папки снаружи (PowerShell / Bash) и потом копируй.

---

## 6. Нарезка на кости — под будущее разрушение

Если модель когда-нибудь надо будет ломать по частям, кости надо заложить **сразу**.
Потом это переделывать больно.

### 6.1. Медианное разбиение (kd-tree)

Резать по фиксированному центру нельзя — куски выйдут неравные (см. 5.3).
Режем по медиане: сортируем кубы по оси, делим пополам, повторяем по следующей оси.

```js
function ctr(e, i) { return (e.from[i] + e.to[i]) / 2; }

function leaves(cubes, axes, idx) {
  if (idx >= axes.length) return [cubes];
  const ax = axes[idx];
  const s = cubes.slice().sort((a, b) => ctr(a, ax) - ctr(b, ax));
  const h = Math.floor(s.length / 2);
  return leaves(s.slice(0, h), axes, idx+1)
    .concat(leaves(s.slice(h), axes, idx+1));
}

// [0,1,2] = резать по X, потом Y, потом Z → 8 кусков
// [0,2]   = по X, потом Z            → 4 куска
const parts = leaves(collect(bodyGroup), [0, 1, 2], 0);

parts.forEach((arr, i) => {
  const g = new Group({ name: 'body_p' + (i+1), origin: bodyGroup.origin.slice() });
  g.addTo(bodyGroup); g.init();
  arr.forEach(c => c.addTo(g));
});
```

Результат на сердце: `13 / 13 / 13 / 13 / 13 / 13 / 13 / 14`. Ровно.

### 6.2. Иерархия: анимация на родителе, разрушение на детях

```
heart_body      ← keyframes биения ЗДЕСЬ
├── body_p1     ← прячем ЭТО при разрушении
├── body_p2
└── ... p8
```

Дочерние кости без своих keyframes просто наследуют трансформ родителя.
То есть можно спрятать `body_p3`, и остальные семь продолжат биться как ни в чём
не бывало. Анимацию переделывать не надо.

Сколько кусков делать: **16–24**. Меньше — разрушение выглядит грубым скачком.
Больше — в коде мода становится неудобно.

---

## 7. Анимация

```js
Modes.options.animate.select();                        // сначала режим!
const anim = new Animation({ name:'heartbeat', loop:'loop', length:1.2 }).add();
anim.select();

function put(group, channel, points) {
  const an = anim.getBoneAnimator(group);
  points.forEach(p => {
    const k = an.createKeyframe({x:p[1], y:p[2], z:p[3]}, p[0], channel, false, false);
    if (k) k.interpolation = 'catmullrom';             // плавно, органично
  });
}

// сердечный ритм «туп-туп»: сильный удар, слабый, отдых
put(heartBody, 'scale', [
  [0.00, 1,     1,     1    ],
  [0.07, 1.10,  1.14,  1.10 ],   // ТУП
  [0.17, 1.01,  0.99,  1.01 ],
  [0.26, 1.07,  1.09,  1.07 ],   // туп
  [0.38, 0.985, 0.975, 0.985],   // отдача
  [0.55, 1,     1,     1    ],
  [1.20, 1,     1,     1    ],   // хвост паузы
]);
```

**Что делает анимацию живой, а не механической:**

- **Y отличается от X и Z.** `1.10 / 1.14 / 1.10` вместо `1.12 / 1.12 / 1.12`.
  Равномерное масштабирование выглядит как надувной шарик.
- **Отдача ниже единицы.** После удара `0.975`, а не сразу `1.0`.
- **Задержка по частям.** Тело бьёт на `0.07`, ветки на `0.11`, корни на `0.15`.
  Волна расходится наружу — читается как передача импульса.
- **Длинный хвост.** Из 1.2 секунды на удары уходит 0.55, остальное — покой.
  Без паузы получается не сердце, а вибрация.
- **`catmullrom`**, не `linear`.

---

## 8. Экспорт под GeckoLib

```js
const dir = 'C:/путь/к/папке';

// геометрия
Blockbench.writeFile(dir + '/model.geo.json',
  { content: Codecs.bedrock.compile() });

// анимация
Blockbench.writeFile(dir + '/model.animation.json',
  { content: JSON.stringify(Animator.buildFile(), null, 2) });

// текстура
Blockbench.writeFile(dir + '/model.png', {
  content: 'data:image/png;base64,' + Texture.all[0].getBase64(),
  savetype: 'image'
});

// исходник проекта
Blockbench.writeFile(dir + '/model.bbmodel',
  { content: Codecs.project.compile() });
```

Раскладка в моде:

```
src/main/resources/assets/<modid>/
├── geo/model.geo.json
├── animations/model.animation.json
└── textures/entity/model.png
```

Проверить экспорт снаружи (PowerShell):

```powershell
$g = Get-Content .\model.geo.json -Raw | ConvertFrom-Json
$g.'minecraft:geometry'[0].description
$g.'minecraft:geometry'[0].bones | ForEach-Object { $_.name }
```

Одна грань должна выглядеть так — по-гранево, а не box UV:

```json
"uv": { "north": { "uv": [96, 0], "uv_size": [5.83, 5.83] }, ... }
```

---

## 9. Как применить к мутировавшему зомби / корове

Тот же конвейер, меняется только SDF и структура костей.
Ниже — как я бы это строил. На сердце проверено всё, кроме конкретных формул из
этого раздела; сам подход тот же.

### 9.1. Общий приём для гуманоида/животного

Тело — не одна SDF, а **набор частей**, каждая со своим центром и своим генератором.
Не пихай всё в одну функцию: части нужны отдельными группами, иначе не будет костей.

```js
const parts = [
  { name:'head',      c:[0, 26, 0], sdf: headSDF,  S:2.5 },
  { name:'torso',     c:[0, 16, 0], sdf: torsoSDF, S:3   },
  { name:'arm_left',  c:[-6, 17, 0], sdf: limbSDF, S:2   },
  { name:'arm_right', c:[ 6, 17, 0], sdf: limbSDF, S:2   },
  { name:'leg_left',  c:[-2,  6, 0], sdf: limbSDF, S:2.5 },
  { name:'leg_right', c:[ 2,  6, 0], sdf: limbSDF, S:2.5 },
];
```

Каждую часть — в свою группу. Это сразу и кости для анимации, и куски для
разрушения. Ходьбу потом анимируешь поворотом `arm_*` / `leg_*`.

### 9.2. Конечность как капсула

```js
function segment(px,py,pz, ax,ay,az, bx,by,bz, r) {
  const dx=bx-ax, dy=by-ay, dz=bz-az;
  const wx=px-ax, wy=py-ay, wz=pz-az;
  let t = (wx*dx + wy*dy + wz*dz) / (dx*dx + dy*dy + dz*dz);
  t = Math.max(0, Math.min(1, t));
  return Math.hypot(wx - dx*t, wy - dy*t, wz - dz*t) - r;
}
```

Рука = `segment(...плечо..., ...локоть..., 2.5)` объединить с
`segment(...локоть..., ...кисть..., 1.8)`. Толщина падает — конечность сужается.

### 9.3. Что делает форму «мутировавшей»

Обычный зомби — это симметричные коробки. Мутант — это сломанная симметрия.
Четыре приёма, по силе воздействия:

1. **Асимметрия.** Не отражай левую сторону в правую. Дай каждой конечности свой
   `seed` и разные пропорции: одна рука толще в 1.6 раза и длиннее на 20%.
   Это самое сильное средство, оно одно уже читается как «что-то не так».

2. **Опухоли поверх силуэта.** После основной формы добавь 3-6 сфер разного
   радиуса в случайных точках поверхности, объединяя через `Math.min`.
   Не по всему телу — кучно, на одном плече или на спине.

3. **Прорастание.** Тот самый `grow()` из раздела 3.4. Из плеча, из спины, из
   рёбер. Параметры «толстые артерии» дают щупальца, «обломки костей» — шипы.

4. **Ассиметричный шум.** Амплитуду шума в SDF сделай зависимой от позиции:
   слева `0.06` (почти нормально), справа `0.20` (разъедено).
   ```js
   const amp = 0.06 + 0.14 * Math.max(0, u);   // u — нормализованный X
   return base + (h(x,y,z) - 0.5) * amp;
   ```

### 9.4. Корова — что меняется

Четвероногое: торс горизонтальный (эллипсоид, вытянутый по Z), четыре ноги-капсулы
вниз, голова смещена вперёд по Z, вымя снизу как сфера.

Для мутации: вздутый торс (радиус по Y больше нормы в 1.4), провисшая шея
(добавь наклон к голове), лишняя нога-обрубок, наросты на боку.

Кости под анимацию: `body`, `head`, `leg_fl`, `leg_fr`, `leg_bl`, `leg_br`, `udder`.
Кости под разрушение — kd-нарезка каждой части, как в 6.1.

### 9.5. Держи семью моделей единой

Если зомби, корова и сердце — из одного заражения, они должны выглядеть роднёй:

- **Одна палитра текстур** на всех. Буквально те же `base` цвета из раздела 4.1.
  Максимум — добавь по одной специфичной плитке (шкура для коровы, тряпьё для зомби).
- **Один и тот же тип отростков.** Если у сердца корни `th 2.8, dec 0.9, wob 0.5` —
  пусть из зомби растут такие же. Глаз это ловит.
- **Одна плотность.** Держи `S = 4` везде. Если у сердца ячейка 4, а у коровы 2,
  корова будет выглядеть из другой игры.
- **Один атлас на всех**, если формат позволяет. Тогда и в моде одна текстура.

---

## 10. Готовый промпт для другого Claude Code

Скопировать и отдать вместе с этим файлом:

```
У меня подключён Blockbench MCP. Сделай модель <ЧТО ИМЕННО>.

Читай MODEL_GENERATION_GUIDE.md и работай строго по нему:

- Форму строй генератором через risky_eval, не расставляй кубы по одному.
- SDF + извлечение оболочки, ячейка S = 4. Не заливай объём целиком.
- После КАЖДОЙ генерации делай capture_screenshot и смотри на результат.
- Отростки — функцией grow(), шаг = th * 0.6, не больше.
- Текстуры: 5 штук одной палитры, раскидать плавным шумом с ранжированием.
- Обязательно cube.mapAutoUV() на каждом кубе, иначе текстуры не будет видно.
- Нарежь на 16-24 кости медианным kd-разбиением, дочерними к анимируемым.
- Держись бюджета 250-350 кубов.

Формат: <java_block | bedrock для GeckoLib>.
Целевой размер: <сколько блоков>.

Перед началом спроси меня про формат и размер, если не уверен.
Отдельно прочитай раздел 5 «Ловушки» — там семь вещей, которые ломаются молча.
```

---

## 11. Чеклист

- [ ] Формат выбран **до** генерации, ограничения проговорены вслух
- [ ] Форма — генератором, не руками
- [ ] Оболочка, а не залитый объём
- [ ] Шум в SDF есть (иначе форма пластиковая)
- [ ] Скриншот после каждого шага
- [ ] `mapAutoUV()` вызван для каждого куба
- [ ] Шаг отростков меньше толщины
- [ ] Текстур ≥ 3, одна палитра, распределены ранжированием
- [ ] 16-24 кости, медианная нарезка, дети анимируемых костей
- [ ] Анимация: неравномерный масштаб, задержка по частям, длинный хвост
- [ ] Атлас собран **до** конвертации формата
- [ ] Бюджет кубов 250-350
- [ ] Экспортированы `.geo.json` + `.animation.json` + `.png` + `.bbmodel`
- [ ] Проверено снаружи: имена костей, per-face UV в geo.json
```

---

*Составлено по итогам сборки `rotten_heart` — 297 кубов, 23 кости, 7 текстур,
анимация 1.2 сек. Все числа и код в разделах 1-8 взяты из рабочей модели.*
